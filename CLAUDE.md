# ROKEY-Team-Project — 작업 원칙

이 파일은 매 세션 자동으로 다시 로드된다. 대화 내용은 세션 종료
또는 컴팩션 시 사라지므로, 매번 반복 적용되어야 하는 원칙만 여기
둔다. 상세 설계는 설계 문서(저장소 루트의 `재활용로봇_설계문서_*.txt`)
를 참고할 것.

## 하드웨어 전제

- 로봇: 두산 M0609 (eye-in-hand RealSense, TCP에 그리퍼와 함께 장착)
- 홈 자세: J3=90°, J5=90°, 카메라가 작업 평면을 수직 하방 조망
- **[팀결정 — v22] 고정 카메라(eye-to-hand)는 RealSense가 아니라
  Logitech C270 HD Webcam**(RGB만, depth/IR 없음, 720p MJPEG).
  손목 카메라(eye-in-hand)만 RealSense 유지. C270은 depth가
  없어 **평면교차가 선택이 아니라 유일한 3D 산출 경로**이고,
  카메라 내부 파라미터를 `camera_calibration` 패키지로 직접
  캘리브레이션해야 함(RealSense처럼 공장값 `camera_info` 없음).
  "무라벨 투명체 depth 결손 검출"(3단계 확장 목표)은 이 카메라로
  원리적으로 불가능해짐. [실측] USB 연결·캡처 확인 완료
  (`/dev/video0,1`, 최대 1280x960).
- **[실측 — 2일차] C270 내부 파라미터 캘리브레이션 완료.**
  `ros-jazzy-usb-cam`/`ros-jazzy-camera-calibration` 패키지 설치
  후(이전엔 미설치였음), 1280x720 MJPEG 30fps + 체커보드(10x7
  내부코너, 25mm/칸) 90샘플로 계산. 결과: `fx=1427.5, fy=1429.8,
  cx=674.3, cy=359.2`, 왜곡계수(plumb_bob) `[0.0838, -0.0067,
  -0.0059, 0.0080, 0]`. 저장 위치: `~/Downloads/c270_calibration_ost.yaml`.
  **[실측] YUYV는 720p에서 7.5fps로 제한, MJPEG만 720p 30fps
  가능** — MJPEG 필수 근거 확인. `/dev/v4l/by-id/` 경로로 C270이
  다른 웹캠과 명확히 구분됨(`usb-046d_C270_HD_WEBCAM_...`).
- **[전달받음 — 2일차, 미확정] 팀이 웹 Claude와 "C270→관제 PC,
  RealSense→로봇팔 PC로 각각 다른 물리 PC에 연결" 방향을 논의했다
  — 아직 정식 설계 메모로 전달받지 않아 CLAUDE.md/설계문서에
  구조 변경을 반영하지 않았다.** 다음 웹 Claude 메모에서 3-1a절
  PC 배치 갱신 여부를 확인할 것.
- 그리퍼: OnRobot RG2, 최대 개폐폭 110mm ([원문] `onrobot.py`의
  `max_width=1100`, 0.1mm 단위)
- 그리퍼 제어: **[실측 — 1일차] ROS2 래퍼 제공됨.**
  `onrobot_rg_control` 패키지의 `OnRobotRGControllerServer` 노드가
  ws_dsr 워크스페이스에 이미 빌드되어 있음.
  서비스: `/onrobot/sendCommand`(`onrobot_rg_msgs/srv/SetCommand`,
  열기/닫기/파지력 증감/목표폭), `/onrobot/pose`
  (`onrobot_rg_msgs/srv/GripperPose`, 관절각→개방폭 조회).
  상태: `/onrobot_joint_states`(실제 하드웨어 값)
- **pymodbus는 3.10 미만이어야 함**(실사용 3.9.2). 3.10+부터
  `read_holding_registers()`의 `slave` 파라미터가 `device_id`로
  이름이 바뀌어 `onrobot_rg_control` 패키지 및 corecode의
  `onrobot.py`/`modbus.py`(둘 다 구버전 API 기준)와 호환 안 됨.
  설치: `pip install --break-system-packages "pymodbus<3.10"`
  ([실측] corecode `onrobot.py`의 `RG.__init__`도 `ModbusTcpClient`에
  `stopbits`/`bytesize`/`parity`/`baudrate` 인자를 넘기는데 현재
  pymodbus에서 `TypeError` 발생 — 같은 구버전 API 문제의 또 다른
  증상. corecode를 직접 쓸 땐 `ModbusTcpClient(ip, port=..., timeout=1)`
  형태로 직접 연결하는 우회가 필요)
- **개방폭 읽기(`get_width` 계열)는 레지스터 3개로 나뉜다**
  ([원문] `onrobot_rg_control`의 `User_Manual.pdf`): 267=Actual
  width(핑거팁 오프셋 미반영, 알루미늄 핑거 안쪽 기준), 275=Actual
  width with offset(오프셋 반영), 258=Fingertip offset(자동 감지
  아님 — 레지스터 1031로 **사용자가 직접 설정·저장**하는 값,
  캘리브레이션 이력 없으면 0일 가능성 높음). 현재 `/onrobot/pose`
  경로는 267 기반이라 오프셋 미반영 — 중간 폭 대역 오차(8.3mm)의
  원인 후보지만 258 실측 전까지 확정 아님.
- **[실측] 컨베이어는 단순 DC모터가 아니라 스테퍼 모터 +
  범용 "Microstep Driver"(PUL/DIR/ENA 방식, 9~42VDC)다.** 기존
  소스코드/문서 전혀 없었음 — 아두이노 Uno에 진단용 스케치를
  직접 작성해 확인함([팀 승인] 하에 진행, CLAUDE.md "구현 코드
  금지" 원칙의 예외 — 최종 `conveyor_node`가 아닌 1회성 진단
  목적임을 항상 명시할 것). 배선: PUL+→D3, DIR+→D4, PUL-/DIR-
  공통→GND. **ENA+는 D5에 물려있지만 펌웨어에서 절대 건드리면
  안 됨**(HIGH/LOW 어느 쪽으로 구동해도 안 움직였고, 핀을
  아예 안 건드려 플로팅 상태로 둬야 정상 동작함). 드라이버
  DIP 스위치(SW1/SW2/SW3)는 **정확히 1개만 ON**이어야 동작(2개
  이상 겹치면 무반응) — 현재 SW1만 ON. 방향은 드라이버 실크스크린
  화살표 표시의 **반대**가 실제 정방향. 시리얼 프로토콜은
  9600 baud, `"<steps_per_sec>\n"`(0=정지)로 직접 정의함
  (기존 규약 없었음). 아두이노 개발에는 `arduino-cli`(snap)를
  씀 — snap 샌드박스가 `/tmp` 경로에 접근 못해 스케치는 반드시
  `$HOME` 하위에 둘 것, USB 접근에 `sudo snap connect
  arduino-cli:raw-usb` 필요(serial-port 인터페이스는 이 환경에
  슬롯이 없어 연결 불가 — raw-usb만으로 업로드는 됨).
- **[팀결정, 2일차 근거 갱신] PC 2대 구성**: 관제 PC(**C270**
  고정 카메라 + 추적 + **컨베이어 제어**) / 로봇 PC(RealSense
  손목카메라·팔·그리퍼·음성·상태머신). **[정정 — v28] 컨베이어를
  관제 PC가 맡는 이유였던 "추적 신뢰도 저하 시 즉시 정지"는
  더 이상 유효하지 않다** — 그 근거였던 `BELT_PAUSE_CORRECT`가
  v23에서 폐기됐다. **현재 유효한 근거는 손 검출 안전정지**
  (고정 카메라가 검출하고 관제 PC 로컬에서 즉시 `conveyor_node`
  를 정지시킴, 네트워크 왕복 없음) — 반대로 음성 정지·종료
  명령은 로봇 PC에서 네트워크를 건너 컨베이어에 도달하는
  비대칭 구조다(수백 ms 지연은 무해). USB 대역폭 분리 근거도
  약화됐다(C270이 720p MJPEG이라 부담이 작음, 3-1a절 상세).
  **[실측 — 2일차] arduino-cli는 현재 로봇 스택과 함께 떠 있는
  공유 개발 PC 한 대에 설치돼 있음** — 2일차 시점엔 물리적으로
  PC가 아직 하나뿐이고(케이블을 번갈아 꽂아 테스트 중), 실제
  2대 분리는 아직 집행 전이다(재활용로봇_설계문서 3-1a절).
- **[팀결정 — v26, 최종 확정, v22/v23/v25 무효화] 분리수거 통은
  5개**(플라스틱/캔/종이/**건전지**/**확인 필요**), 로봇 양옆
  배치. **`battery`는 전용 통으로 로봇이 직접 파지해 넣는다**
  (v22의 "사람 전달" 결정 폐기). `styrofoam`은 품목에서 **제외**
  (파지력 하한 시연은 `can`으로 대체). **`vinyl_bin`(비닐 전용)이
  `review_bin`(확인 필요)으로 바뀌었다** — 재질별 분류(4통)와
  처리상태별 분류(1통)가 섞인 비대칭 구성은 결함이 아니라 "로봇이
  자기 한계를 아는 시스템"이라는 설계 의도를 물리적으로 드러내는
  장치([팀결정], 재론 안 함). `review_bin`에는 "확인 필요" 물리
  라벨을 부착한다. `plastic_bag`은 **묶인 것만 소품으로 준비**
  (낱장 `plastic_bag_loose`는 폐기 — 소품 자체를 준비하지 않음)
  하고 **무게 측정 없이 바로 `review_bin`으로 이동**한다(비전·
  무게 둘 다로 내용물 판별 불가한 유일한 품목이라는 게 근거).
  **클래스 수는 6개로 축소**(styrofoam 제외 + plastic_bag_loose
  폐기, 이름은 `plastic_bag_bundled`→`plastic_bag`으로 환원).
  **`PLACE_BACK_ON_BELT`/`place_back_pose`/로봇 옆 `general_bin`
  대안은 모두 폐기** — review_bin 도입으로 파지 후 되놓기 경로
  자체가 사라짐(무게측정 결과 무거운 opaque_container도 review_bin
  으로 직접 파지). 벨트 끝 일반쓰레기통은 **미검출/미학습 물체
  전용**으로 남는다(6클래스 밖 물체는 학습시키지 않고 그대로
  통과 — 판정이 바뀌지 않으므로, CLAUDE.md 원칙7)
  (재활용로봇_설계문서 3-5/3-5a/4-1c/4-2a절 v26 개정).
- **[확정 — v25] 건전지는 AA로 진행, D형 폴백 불필요.** 조정된
  카메라 구도(30~40도 각도, 배경 차폐)에서 실측 폭 약 30px/길이
  약 110px로 20px 검출 기준을 상회함. 은박지류와 색·광택이
  유사해 혼동 가능성이 있어 건전지 촬영량은 다른 품목보다 다소
  늘릴 것.
- **[실측 — 2일차] `can` 파지력 하한: 정적 매달림(로봇 미이동,
  중력만) 조건에서 5N→2.5N→0N(그리퍼 소프트웨어 명령 최저값)까지
  내려도 미끄러지지 않았다** — 이 캔+고무 재질 핑거팁 조합에서는
  중력만으로 하한을 특정할 수 없었다. 단 0N 명령 상태에서도
  사람이 손으로 당기자 캔이 찌그러지려 했다 — **그리퍼의 "0N"
  명령이 실제 물리적 0N을 의미하지 않는 것으로 추정**[추론].
  상한(찌그러짐)과 하한 사이 여유가 기대보다 좁을 가능성이
  있어, **상한 실측(캔 여분 확보 후) 우선순위가 높아졌다.**
- **[제외 — v25, 팀결정] PET병(`pet_bottle`) 오염 판정을 설계에서
  제외한다.** 근거: 재현 가능한 오염 샘플 제작이 어렵고(투명
  용기에 물을 넣으면 "오염"이 아니라 "내용물"로 보임, 색 액체는
  쏟을 위험), 무게 측정과 역할이 겹치고(`opaque_container`에서
  이미 "내용물 있으면 일반쓰레기"를 보여줌), 손목 카메라 촬영
  계획이 이 판정 하나 때문에 통째로 잡혀 있었다. `pet_bottle`은
  이제 라벨 유무만으로 분기한다(라벨 있음→사람 핸드오버, 없음→
  자동 재활용). 손목 카메라는 역할이 줄어도 존치한다 — eye-to-
  hand/eye-in-hand 두 캘리브레이션 과제 충족 + 파지성공 교차검증
  근거(재활용로봇_설계문서 4-3절).
- **[팀결정 — v23] 벨트는 위험 상황에만 정지한다** — 사람 손
  검출/힘 하드리밋/음성 정지 명령/작업 종료뿐. **미검출/미학습
  물체, 파지 실패, 판정 애매, 추적 신뢰도 저하로는 벨트를 세우지
  않는다**(`BELT_PAUSE_CORRECT`, `plastic_bag` ASSIST 정지 모두
  폐기 — v22의 "plastic_bag 감지 시 벨트 정지" 결정도 무효).
  실시간 사람 개입(ASSIST)은 **라벨 있는 페트병 핸드오버 하나만**
  남는다 — 나머지(review_bin행 물체, 미학습 물체 등)는 기록만
  남기고 작업 종료 시 음성으로 일괄 보고한다. **[수정 — v27]
  확인 필요 사유(내용물 있음/판별 불가)는 종료 보고가 아니라
  물체를 쥔 순간(REPORT 단계)에 짧게 말하고, 종료 보고는 문단2
  (확인 필요 건수)/문단3(핸드오버 실패 건수 — 물체 문제 아닌
  운영 상황이라 별도 분리)로 나뉜다**(재활용로봇_설계문서 3, 4,
  6-2a/6-4절 v23/v26/v27 개정).
- **[팀결정 — 실측 기반] 컨베이어 속도는 100 steps/sec로 고정**
  (50=무동작, 100=적당함 확인, 200=과속으로 사용자 판단).
  속도 조절 기능은 지금 만들지 않는다 — **기본 동작 구현을 먼저
  마무리하고 여건 될 때 나중에 추가**하는 순서로 간다. 수동
  on/off는 `~/bin/conveyor {on|off|rev|<steps_per_sec>}` 스크립트로
  가능(직접 작성한 진단용 헬퍼, `conveyor_node`ROS2 구현체가
  아님).
- **[실측 — 2일차, 완전 종료] 컨베이어 탈조 검증 완료 — 100
  steps/sec는 탈조 없음 확정.** DIP표 실측(사진, SW1만 ON):
  **400 pulses/rev, microstep 2/B**. 풀리 지름 실측: **2.5cm**
  (둘레 π×2.5=7.85cm). 이론 속도 vs 실측: 100 steps/sec는
  1.96cm/s(이론) vs 2.0cm/s(실측, 차이 2%, 오차범위 내) — 탈조
  없음. 200 steps/sec는 3.93cm/s(이론) vs 2.5cm/s(실측, 차이
  36%) — 탈조 확정(200은 시연에 안 쓰여 무관). **칼만 필터
  상수는 실측값 2.0cm/s를 그대로 쓴다.**
- **[실측] 그리퍼가 "Connected successfully" 로그 후에도
  `ModbusIOException: No response received after 3 retries`로
  죽는 경우가 있다** — TCP 연결은 되는데 장치가 응답을 안 하는
  상태. **그리퍼 Compute Box 전원을 껐다 켜면 해결됨.** pymodbus
  버전 문제나 Modbus 포트 충돌과는 다른 별개의 실패 모드다.
- **[실측 — 2일차] `/onrobot/restartPower` 서비스(`std_srvs/Trigger`)
  호출하지 말 것 — 즉시 노드를 죽인다.** `restartPowerCycle()`이
  `self.client.write_register(address=0, values=message,
  slave=restart_address)`를 호출하는데, 현재 pymodbus 버전은
  `values` 키워드 인자를 받지 않아(`TypeError`) 그 자리에서
  크래시한다(`onrobot_rg_control`/`comModbusTcp.py` 88행,
  `OnRobotRGControllerServer.py` 207행). 그리퍼가 무응답일 때
  이 서비스로 "제어권 회수"를 시도하지 말고 Compute Box 전원을
  직접 껐다 켤 것.
- **[실측 — 2일차, 문서 정정] 그리퍼가 응답 없어 보여도 하드웨어
  문제가 아닐 수 있다 — 토픽 리맵 누락이 원인이었던 사례 확인.**
  `onrobot_rg_control`의 `OnRobotRGControllerServer`는 조인트
  상태를 항상 **`/joint_states`**로 퍼블리시한다(소스에
  하드코딩, `remap`으로만 이름을 바꿀 수 있음) — `/onrobot_joint_states`
  로 직접 퍼블리시하지 않는다. 아래 수동 실행 명령에 **`-r
  /joint_states:=/onrobot_joint_states` 리맵이 빠지면**, 노드는
  "Connected successfully"까지 정상 출력하고 그리퍼 제어(`/onrobot/
  sendCommand`, `/onrobot/pose`)도 되지만 **`/onrobot_joint_states`
  토픽엔 아무 것도 안 뜨고 에러도 없이 조용히 비어있다** — 이
  증상은 Modbus 무응답과 겉보기엔 비슷하지만 원인이 다르다.
  `ros2 topic echo /onrobot_joint_states`가 계속 멈춰 있으면
  먼저 `ros2 node info /OnRobotRGController`로 실제 퍼블리시
  토픽명을 확인할 것.
- **[운영] 로봇 제어 스택이 꺼져 있으면(`ros2 node list`에
  `/dsr01/*` 안 뜨면) 아래로 재기동**:
  ```
  ros2 launch m0609_rg2_bringup bringup.launch.py mode:=real host:=192.168.1.100 model:=m0609
  ```
  **[정정 — 2일차]** `src`/`install` 양쪽의 현재 `bringup.launch.py`
  (149~167행)에는 그리퍼 노드(`onrobot_driver`, `/dsr01` 네임스페이스,
  `/joint_states→/onrobot_joint_states` 리맵 포함)가 `mode:=real`
  조건으로 이미 **포함돼 있다** — 과거 "그리퍼 노드를 포함하지
  않는다"는 기록은 더 이상 정확하지 않다(teammate가 이 launch
  파일을 갱신한 것으로 추정, 정확한 시점은 git log로 확인 필요).
  **다만 2일차 세션 시작 시 이미 떠 있던 `/dsr01/*` 스택에는
  그리퍼 노드가 없었다** — 이 파일이 갱신되기 전에 띄워진
  것으로 보인다. **[확인 — 2일차] 스택을 처음부터 재기동하면
  위 명령 하나로 그리퍼까지(`/dsr01/OnRobotRGControllerServer`)
  자동으로 뜨는 것을 실측 확인함** — 별도 그리퍼 실행이 더 이상
  필요 없다. 스택 재기동 없이 그리퍼만 따로 다시 띄워야 하는
  경우(예: 그리퍼만 죽었을 때)에만 아래처럼 **반드시 리맵을
  붙여서** 수동 실행:
  ```
  ros2 run onrobot_rg_control OnRobotRGControllerServer --ros-args \
    -p /onrobot/gripper:=rg2 -p /onrobot/control:=modbus \
    -p /onrobot/ip:=192.168.1.1 -p /onrobot/port:=502 \
    -r /joint_states:=/onrobot_joint_states
  ```

## 설계 원칙 (위반 시 즉시 지적할 것)

1. **크기로 클래스를 구분하지 않는다.** eye-in-hand top-down에서
   픽셀 크기는 카메라-대상 거리에 의존적이며, RealSense depth
   오차(±1~2mm)로는 보정 불가. 실루엣 1순위, 색상 2순위.
2. **OCR에 의존하지 않는다.** 텍스트 판독이 필요하면 DB 조회로
   우회한다.
3. **외부화 원칙.** 클래스 이름과 물리 수치는 코드에 하드코딩하지
   않고 YAML로 분리한다. 판정 로직에 품목명에 대한 분기를 두지
   않는다 — `clarification_rules[material_type].trigger`처럼
   설정을 조회해서 동작해야 한다.
4. **페이로드 6kg 제약** 내에서 대상을 선정한다.
5. **재현성 우선.** 기능 추가 여부는 "5일 안에 10회 시연해서
   10회 성공 가능한가"로 판단한다.
6. **과대 청구 금지.** 구현한 것 이상으로 표현하지 않는다.
   예: 규칙 덮어쓰기를 "학습"이라 부르지 않는다.
7. **판정을 바꾸는가를 먼저 묻는다.** 새 기능을 검토할 때
   "구현 가능한가"보다 "이 기능이 판정이나 동작을 실제로
   바꾸는가"를 먼저 확인한다. 판정 결과가 같다면 구현
   난이도와 무관하게 제외 대상이다. (예: 기울임 판별 —
   액체든 고체든 내용물이 있으면 어차피 일반쓰레기라 판정이
   바뀌지 않아 v9에서 제외)

## 문서 작성 규칙

- [원문]/[추론]/[전제] 구분 표기를 항상 유지한다.
- 저장소 파일을 인용할 때는 경로와 행 번호를 함께 밝힌다.
- 확인하지 않은 것을 확인한 것처럼 쓰지 않는다.
- **[원문]으로 인용할 때는 값 자체뿐 아니라 그 값이 어떤 맥락에서
  나온 것인지도 함께 확인한다.** 숫자가 문서에 존재한다는 것과
  그 숫자가 우리가 쓰려는 의미인 것은 다르다. (예: README 279행의
  "±2N"은 노이즈 바닥이 아니라 compliance 미활성 시 값이 갱신되지
  않던 버그 증상이었음 — v5~v9의 설계 전제가 이 오해 위에 있었다)

## 안전

- 힘 임계값은 스쿠핑 프로젝트 값(±20N)을 그대로 쓰지 않는다.
  구조(감지→즉시 해제→HOLD)만 재사용하고 수치는 실측한다.
- Fy는 스쿠핑 프로젝트에서 하드리밋 대상이 아니었다. 3축 모두
  측정 후 포함 여부를 판단한다.
- 안정화 확인(투입 감지, PICK 직전 재확인)은 보조 수단이며,
  최종 안전망은 힘 하드리밋(HOLD)이다.
- **[실측] TCP 힘 한계는 160N(일반 모드)이다 — 순응 제어 중
  살짝 당겨도 SAFE_OFF에 걸리는 현상(1일차 관측)은 이 힘
  하드리밋과 무관하다.** (과거 CLAUDE.md에 "5~9N에서 하드리밋"
  이라 잘못 기록했던 적 있음 — 그 값은 힘 한계가 아니라 관측된
  SAFE_OFF 발생 구간일 뿐이었다. 실제 원인은 [추론] 관절 각도
  허용치(±3.0°, 전 관절 공통) 초과 또는 충돌 민감도(30%) 감지로
  재추정 중이나 확정을 포기했다 — SAFE_OFF는 항상 티치펜던트
  팝업을 동반하지 않음을 실측으로 확인해([실측 — v25] push
  방향 재현 시 팝업 없이 상태만 바뀜) 에러 메시지로 원인을
  확정한다는 접근 자체를 **종료**했다. 대신 아래 검증 기준으로
  대체한다.) 순응 핸드오버는 힘 기반보다 **위치(TCP 변위) 기반
  감지**를 우선 검토할 것 — 관절 엔코더가 힘 센서보다 정밀하고
  한계값(관절 3°≈TCP 약 26mm)이 명시돼 있다.
- **[실측 — v25] SAFE_OFF는 미는(push) 방향에서만 재현됐고 당김
  (pull) 2회는 무사고였다.** 이번 재현은 고강성(3000)이었고
  최초 성공 사례(v11)는 저강성(500)이었다 — **조건이 다르다.**
  핸드오버는 사람이 당기는 동작이므로, 실제 시나리오(저강성+
  당김)에서는 발생률이 낮을 가능성이 있다[추론]. 이 관찰이
  아래 검증 기준의 조건을 결정했다.
- **관절 가동 범위가 물리적 한계보다 좁게 설정되어 있다**: J2
  ±95°(물리 -360~360), J3 ±135°(물리 -150~150), J5 ±135°(물리
  -360~360). J1/J4/J6은 제한 없음. 홈 자세(J3=90°, J5=90°)는
  범위 안. 분류 통 배치·측정 자세·핸드오버 자세·벨트 접근 자세를
  정할 때 이 범위(특히 J2가 가장 좁음)를 고려할 것.
- **SAFE_OFF 복구가 항상 ROS2 API(`set_robot_control`
  `CONTROL_RESET_SAFET_OFF`)만으로 되는 것은 아니다.** 1일차에
  API 호출은 `success=True`를 반환했지만 실제 상태가 안 풀린
  사례가 있었고, 이때는 **티치펜던트에서 오류 팝업을 직접 닫고
  "서보 켜기"를 눌러야** 복구됐다.
- **[실측 — 2일차] 티치펜던트에서 물리적으로 복구(서보 켜기)해도
  `ros2 service call .../get_robot_state`는 한동안 계속 SAFE_OFF
  (`robot_state=3`)로 보고할 수 있다** — `get_last_alarm`은
  정상(빈 로그)인데 `get_robot_state`만 계속 SAFE_OFF로 나온
  사례 확인. **`dsr_controller2`(=`ros2_control_node`) 자체를
  재시작(로봇 스택 전체 재기동)해야 ROS2 쪽 상태가 실제 상태와
  다시 일치**했다. 즉 **"펜던트 화면이 정상"이라는 사람의 확인과
  ROS2 상태 조회 결과가 다르면, 펜던트 쪽을 신뢰하고 ROS2 상태는
  스택 재기동 전까지 stale할 수 있다고 가정할 것.**
- **[팀결정] SAFE_OFF 자동 복구는 설계 목표로 삼지 않는다.**
  API 복구가 신뢰할 수 없음이 실측으로 확인됐으므로, "복구
  경로를 자동화한다"가 아니라 **"SAFE_OFF 자체가 발생하지 않게
  한다"**를 목표로 한다. 발생하면 사람이 복구한다. **[조건 특정
  — v25] 검증 기준**: **병진 강성 500(저강성) + 당김(pull) 방향
  + 핸드오버 자세 + 라벨 페트병** 조건으로 순응 핸드오버를 10회
  연속 시행해 0회 SAFE_OFF여야 시연에 포함한다(9일차 리허설).
  조건은 항상 함께 기록할 것 — "10회 연속"만 적으면 고강성이나
  미는 방향으로 다시 테스트할 여지가 남는다. **[표현 정정 — v26,
  과대 청구 금지 원칙 적용]** 10회 무사고는 95% 신뢰도로 "발생률
  30% 미만"이라는 **약한 보장**일 뿐 "발생하지 않는다"는 확정이
  아니다 — 문서·발표에서 "시연 가능한 수준으로 낮다"로만 표현할
  것, "안전이 확정됐다"는 표현은 쓰지 않는다.
- **[실측 — 2일차] 위와 동일 조건(강성500+당김)으로 예비 1회
  시행 — SAFE_OFF 없이 TCP 변위 36.8mm까지 도달, `get_current_posx()`
  0.3초 폴링 75회 전부 정상 응답.** 위치 기반 감지의 실시간
  갱신 전제조건이 충족됐고, 잠정 release 임계값(10mm)이 이 여유
  안에 들어옴을 확인. **단 핸드오버 자세 미티칭·정식 대상(라벨
  페트병) 아닌 예비 시행이라 9일차 정식 10회 카운트에는 미포함.**

## 현재 단계

- 주제: **컨베이어 기반 재활용 분류 협동로봇** (확정, 트레이
  투입 방식 아님 — 벨트 이송 + 동적 추적 + 예측 파지)
- 평가 기준: 참신성·사업성 미평가, **기술력만 평가**
  (완성도 + 기법 융합 + 설명 가능성). "왜 음성이어야 하는가"류
  방어 논리는 더 이상 판단 기준이 아님 — 대신 각 기술 선택마다
  "왜 이 방법이어야 했는가"를 설명할 수 있어야 함
- **[팀결정 — 3일차] 강사 승인 완료. 구현 단계 진입. 구현 코드
  작성 제약 없음.** (과거 "강사 승인 전 [전제] 상태"/"설계·
  인터페이스·설정 스키마까지만, 구현 코드 작성 금지"는 이
  시점부로 무효. 단 실측 미완을 이유로 붙은 개별 [전제] 표기는
  그대로 유지 — 이건 승인과 무관하게 아직 안 잰 것일 뿐임.)
  컨베이어 진단 펌웨어([팀 승인] 예외로 작성됐던 것)도 이제
  예외가 아니라 정식 구현 대상이다 — 단 `conveyor_node`는
  워치독·서비스 인터페이스를 갖춘 별도 구현이어야 하므로 진단
  펌웨어를 그대로 승격시키지 않는다.
- 최신 설계 문서: 저장소 루트의 `재활용로봇_설계문서_v32.txt`
  (버전 번호가 더 높은 파일이 최신)
- **[완료 — 3일차 심야] end-to-end 정확도 근본원인 규명 + 수정.**
  펜 마킹 실측(8.7~23cm 오차)의 원인은 두 가지였다: (1) corecode
  `eye2hand_calibration.py`의 AX=XB 솔버 자체가 버그(가상 정답
  데이터로 검증, `cv2.calibrateHandEye()`로 교체 완료), (2) 그래도
  남은 오차는 **eye-to-hand 캘리브레이션 샘플의 깊이(카메라로부터
  거리) 다양성 부족** — 회전은 다양했지만(42°) 깊이 범위가
  66mm로 좁아 깊이축 정확도가 안 잡혔고, 벨트처럼 먼 곳에서
  증폭됨(체커보드-그리퍼 오프셋 역산 표준편차: X,Y는 1~2mm인데
  Z만 7.7mm). **진단 방법**: 코너 쌍 사이 거리를 검산해 "예측
  거리/실제 거리(24mm×칸수)" 비율이 위치와 무관하게 0.60~0.62로
  일정함을 확인(강체변환은 거리를 보존하므로 이건 캘리브레이션
  문제가 아니라는 게 결정적 단서였음 — 웹 클로드 v36 지적).
  깊이 다양성을 넓힌 재캘리브레이션은 **체커보드 테이프 마운팅의
  재현성 한계**로 오히려 악화돼 실패 — 대신 실측 5점(펜 마킹)에
  유사변환을 피팅한 경험적 보정(`rocycle_robot/geometry/
  belt_correction.py`)을 `pixel_to_base_point`의 belt_plane 결과에
  후처리로 적용, RMS 4.5mm/최대 8.6mm로 검증 완료(추가 랜덤
  검증 5회 재현, 0~4mm). **[전제] 이 보정은 belt_plane 전용** —
  다른 평면에 쓰려면 재보정 필요.
- **[해소 — 3일차 심야] 벨트 물리 위치 이동 후 원위치 복원 +
  재검증 완료.** 3일차 후반 반복된 로봇 접근 시행 중 벨트가
  밀렸었으나, 원위치 복원 후 랜덤 코너 재검증에서 오차 약 2.2mm
  (복원 전과 동등)로 확인 — `belt_plane`/`belt_correction.py`
  재보정 불필요. 4일차 시작 시 벨트를 추가로 건드리지 않았다면
  이 상태 그대로 신뢰 가능. **다만 "복원 가능했다"는 이번
  1회 검증일 뿐 — 벨트를 클램프/볼트로 물리 고정하는 게 근본
  대책(설계문서 v32 13절 41번, 아직 미착수)이며, 시연 전
  체크리스트에도 포함해야 함.**
- **[신규 — v37] `belt_correction.py` 정밀도가 건전지(AA, 반지름
  7mm) 파지엔 여유가 크지 않음** — 최대 잔차 8.6mm(반지름보다
  큼), RMS 4.5mm(반지름의 64%). 캔·페트병 등 큰 물체는 문제없을
  가능성이 높지만, **건전지 파지는 실측 성공률 확인 또는 추가
  마킹으로 정밀도를 높인 뒤 시도할 것.**
- **[신규 — v37] 캘리브레이션 품질 사전 판정 절차** — 캘리브레이션
  직후·실측 전에 "체커보드-그리퍼 오프셋 역산 표준편차"를 항상
  확인할 것(설계문서 v32 13절 42번). 축별 표준편차 2mm 이하 정상,
  5mm 초과면 재촬영 검토 — 이 검사가 있었다면 2차 캘리브레이션
  문제를 실측 전에 걸렀을 것.
- **[정정 — v37] 1차 캘리브레이션 실패(157mm 오차)의 원인은
  "회전 다양성 부족(4°)"이 아니라 솔버 버그였을 가능성이 높다.**
  1차의 9샘플 데이터를 수정된 알고리즘(`cv2.calibrateHandEye`)
  으로 재계산하면 2차(12샘플, 회전다양성 42°)와 거의 같은 값
  (tilt 38.09° vs 38.35°)이 나옴 — 회전 다양성과 무관하게 같은
  결과가 나온다는 뜻. 3일차 초반 "회전 다양성 부족"으로 오진단
  했던 기록(위 항목들)은 이 정정을 참고할 것.
- **[팀결정 — v31] 클래스 구성 변경.** `opaque_container` 제외,
  무게 측정은 **`can`**으로 이동(캔도 불투명이라 조건 동일, 금속
  이라 850nm 포함 어떤 파장도 투과 못 해 논거가 더 강해짐, 이미
  촬영·라벨링 완료라 추가 촬영 불필요). 소품은 **미개봉 캔**
  채택(시연 중 절대 안 새고 준비가 가장 쉬움 — 팀결정, 시연
  안정성 우선). `pet_bottle`은 **2클래스로 분할**
  (`pet_bottle_labeled`/`pet_bottle_unlabeled`) — 라벨 유무로
  사람전달/자동재활용 경로가 갈리므로 필수. 클래스 수는 6개로
  그대로 유지(구성만 변경). **[미확인]** 이미 촬영한 `pet_bottle`
  이 라벨 있는 것/없는 것 중 어느 쪽인지 팀 확인 대기 중.
- **[신규 — v31] 미개봉 캔은 파지력 실측을 빈 캔과 별도로 해야
  함.** 미개봉은 내압으로 빈 캔보다 단단함 — 기존 실측(3일차,
  강화구간 30N/중앙 12.5N)은 전부 빈 캔 기준이라 그대로 못 씀.
  파지 높이 3단계 × 캔 상태 2종(빈 것/미개봉) = 6조건 재실측
  필요(설계문서 v31 4-4a절).
- **[확인 — v34] 3일차에 쓴 체커보드는 종이(약 0.1mm, 판 마운트
  아님)다** — 벨트 밖으로 걸쳤을 때 처진 것으로 확인. 두께로 인한
  수평오차(0.12~0.17mm)가 무시할 수준이라 오늘 캘리브레이션·
  e2e 결과는 재검토 불필요. 앞으로 펜 마킹 측정 시에도 이 종이를
  벨트에 직접 붙이면 별도 두께보정이 필요 없다.
- **[결정 — v34] obstacles의 통 항목 `z_floor`는 "통 상단/입구"가
  아니라 "통 바닥 높이"다** — 입구 높이로 설정하면 정상 PLACE
  동작(그리퍼가 통 안까지 내려가는 것)이 매번 거부된다. 통 벽
  충돌은 이 규칙으로 못 막지만, 통 접근/배치가 항상 티칭된 고정
  자세로만 이뤄지는(수직 접근) 전제 안에서는 수용 가능.
- **[신규 — v29] 충돌 모델(obstacles) 도입.** 3일차 벨트 압박
  사고의 근본원인은 "얇은 평면 오인"이 아니라 "설계에 물리
  구조물 자체가 정의돼 있지 않았다"는 것(웹 v32 지적, 타당함).
  `rocycle_robot/geometry/obstacles.py`에 최소 구현(경로계획
  아님, 목표점의 xy가 구조물 범위 안이면 z가 `z_floor` 아래로
  못 내려가게 거부) + `config/obstacles.yaml`에 벨트 실측값
  반영 완료. 앞으로 로봇에 임의 좌표로 이동 명령을 내리는 코드는
  이 체크를 거칠 것 — 특히 대각선 접근(수직 하강이 아닌 경로)을
  쓰게 되면 이 최소 구현의 "경로 중간은 안 본다"는 전제가 깨지니
  주의할 것(설계문서 v29 7절/응답문서 1-4-(3)).
- **[실측 — 3일차] 통합측정(작업 B/C/D/E) 결과**: 벨트 평면 5점
  티칭 완료(잔차 최대 0.334mm). eye-to-hand 캘리브레이션은 1차
  (9샘플, 테이프 고정)가 end-to-end 검증에서 약 157mm 오차로
  실패 — **원인은 회전 다양성 부족**(9샘플의 ry가 -153~-157,
  4° 범위 안에서만 변함 — AX=XB 솔브는 회전축이 충분히 달라야
  정확한데 거의 동일 자세만 반복 촬영한 셈이었음)이 결정적,
  체커보드 테이프 고정 헐거움은 부차적 원인으로 추정. 체커보드
  견고 재고정 + 12샘플(ry 42° 범위로 확대)로 재캘리브레이션해
  통과. **재검증은 정성적으로만 완료**(육안상 두 지점 모두
  코너에 거의 정확히 도달, 1칸=25mm 이내로 추정) — 로봇이
  목표에 정확히 도달할수록 카메라 시야에서 손목 몸체가 그
  지점을 스스로 가려 mm 단위 정밀 측정은 못 함. 정밀 재측정이
  필요하면 시각정렬이 아니라 **실접촉(터치) 방식**으로 전환
  할 것. 상세는 설계문서 v28 13절 작업순서 참고.
- **[안전 — 3일차, 재발 방지]** 벨트를 "얇은 평면"이 아니라
  **"테이블면부터 벨트면 높이(7.5cm)까지 롤러/프레임이 차지하는
  입체 구조물"**로 취급할 것 — 접근 높이를 벨트면과 테이블면
  사이 애매한 값으로 계산하면 옆에서 벨트 프레임을 눌러버릴 수
  있다(3일차 실측으로 확인, 안전 후퇴로 대응함). 통(bin) 등 다른
  물리 구조물 접근 경로를 설계할 때도 "면이 아니라 입체"로
  가정할 것.
