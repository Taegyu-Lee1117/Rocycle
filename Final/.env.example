# Copy this file to .env on the Ubuntu control PC.
# Never commit the real .env file.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=recycle_db
DB_USER=recycle_user
DB_PASSWORD=change_me

# The API listens on every interface so another PC on the same LAN can open it.
API_HOST=0.0.0.0
API_PORT=8000
