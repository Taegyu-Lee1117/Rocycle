"""Bin hover/PLACE teaching data — 4일차, 최종본.

라벨 주의: "PAPER"/"PLASTIC"/"CAN"/"BATTERY"/"NOT_EMPTY"는 사용자가 통에
임의로 붙인 물리 스티커 이름이며, 코드/설계문서의 클래스명과 1:1이 아닐
수 있다 — 특히 NOT_EMPTY == review_bin("확인 필요" 통, v26 결정)이다.

Units: mm/deg, DR_BASE frame, matches get_current_posx(ref=0) convention.

[이력] 최초 티칭 이후 두 차례 물리 이동(+70mm, +20mm = 최초 티칭값 대비
총 +90mm, x축)이 있었다 — 로봇 베이스/몸통이 회전하며 통을 밀어버리는
충돌(아래 CLAUDE.md 기록)을 피하기 위함. 그 이동 이후 5개 통 전부
재검증 완료(안전한 "수직 상승 -> 수평 이동 -> 수직 하강" 시퀀스로).

[신규 발견 — 4일차] CAN과 BATTERY는 최종 위치에서 **수직 접근(ry~=180)이
물리적으로 도달 불가능**했다 — PAPER/PLASTIC/REVIEW와 달리 이 둘은
로봇 도달범위 경계에 걸린다(BATTERY는 Y가 커서(~230mm) 같은 X라도
PAPER/PLASTIC/CAN보다 일찍 한계에 부딪힘). 대응:
  - CAN: x=670(원래 티칭값 근처 유지, 통은 옮기지 않음), 손목(ry)을
    수직에서 약 14° 기울여(ry=166) 도달 확보. 자세히는 CLAUDE.md 참고.
  - BATTERY: 기울임 없이 x,y를 원래 목표(633.61, 231.71)에서 각각
    ~13mm/~11mm 부족한 (620.36, 220.36)까지만 도달 — 사용자가 "이
    정도면 통 안에 들어가서 문제없다"고 현장에서 확인, 통을 옮기거나
    추가로 기울이지 않기로 함(2025-09-04 결정, [전제] 통 안쪽 여유가
    넓다는 전제 — 통이 작으면 재검토 필요).
"""

# (physical_sticker_label, design_doc_bin_name, x, y, z, rx, ry, rz)
BIN_HOVER_POINTS = [
    ("PAPER", "paper_bin", 354.08, 9.59, 422.28, 170.11, -179.93, 169.81),
    ("PLASTIC", "plastic_bin", 537.30, 11.49, 422.28, 178.28, -179.78, 177.96),
    ("CAN", "can_bin", 670.0, 12.68, 422.28, 2.66, 166.0, 2.40),
    ("BATTERY", "battery_bin", 620.36, 220.36, 422.28, 1.58, 179.69, 1.10),
    ("NOT_EMPTY", "review_bin", 408.11, 224.34, 422.28, 157.76, -179.78, 157.11),
]

# PLACE z는 통마다 다르다 -- 4일차 실측:
#   PAPER/PLASTIC/REVIEW: 호버(422.28) - 100mm = 322.28 (수직 접근)
#   CAN: 호버(422.28) - 120.1mm = 302.18 (ry=166 기울임 유지한 채 하강)
#   BATTERY: 호버(422.28) - 103mm = 319.27 (기울임 없음, xy는 호버와 동일)
# x, y, rx, ry, rz는 각 통에서 호버와 PLACE가 전부 동일 -- 수직/고정자세
# 하강만 사용했고, 대각선 접근은 쓰지 않았다(아래 시퀀싱 항목 참고).
BIN_PLACE_POINTS = [
    ("PAPER", "paper_bin", 354.08, 9.59, 322.28, 170.11, -179.93, 169.81),
    ("PLASTIC", "plastic_bin", 537.30, 11.49, 322.28, 178.28, -179.78, 177.96),
    ("CAN", "can_bin", 670.0, 12.68, 302.18, 2.66, 166.0, 2.40),
    ("BATTERY", "battery_bin", 620.36, 220.36, 319.27, 1.58, 179.69, 1.10),
    ("NOT_EMPTY", "review_bin", 408.11, 224.34, 322.28, 157.76, -179.78, 157.11),
]

# [중요 — 4일차, 사용자 지적 + 실제 사고로 재확인] 실제 동작 순서에서
# 안전상 핵심은 PLACE 좌표 자체가 아니라 시퀀싱이다: 벨트에서 물체를
# 집은 뒤 (1) 통 벽과 안 부딪힐 만큼 충분히 상승 -> (2) 그 상태로 목표
# 통의 호버 지점까지 수평 이동 -> (3) 그다음에야 PLACE 높이로 하강.
# **상승 없이 통 PLACE 높이에서 바로 다음 통으로 이동하면 그리퍼가 통을
# 밀어버린다** — 4일차 통합 검증 중 실제로 재현됨(문서화 겸 재발 방지
# 목적으로 기록). 이 순서는 obstacles.py의 "최종 목표점의 xy가 구조물
# 범위 안이면 z가 z_floor 아래로 못 내려가게 거부"라는 최소 구현이 세운
# "수직 접근만 가정" 전제와 정확히 대응된다 -- 실제 모션 코드(상태머신)
# 는 반드시 이 3단계 시퀀스를 지켜야 하고, 대각선/직선 이동으로 줄이면
# obstacles.py가 못 잡는 충돌이 발생한다(설계문서 v29 7절에 이미 경고됨).
