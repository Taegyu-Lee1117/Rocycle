# ROKEY-Team-Project — 작업 원칙

이 파일은 매 세션 자동으로 다시 로드된다. 대화 내용은 세션 종료
또는 컴팩션 시 사라지므로, 매번 반복 적용되어야 하는 원칙과 현재
유효한 값만 여기 둔다(v46 정리, 936줄→분리). **상세 실측 데이터와
경위, 무효화된 결정의 이력은 `실측기록.md` 참고.** 상세 설계는
설계 문서(저장소 루트의 `재활용로봇_설계문서_v32.txt`, 버전 번호가
더 높은 파일이 최신)를 참고할 것.

## 하드웨어 전제 (현재 구성)

- 로봇: 두산 M0609, eye-in-hand RealSense(TCP에 그리퍼와 함께
  장착). 홈 자세: J3=90°, J5=90°.
- 고정 카메라(eye-to-hand): **Logitech C270**(RGB만, 720p MJPEG
  30fps 필수 — YUYV는 7.5fps로 제한됨). **[실측 — 8일차, 위반
  발견] `v4l2_camera` 0.7.1이 MJPG 디코딩을 못 해 YUYV(7.5fps)로
  우회한 게 `readme2.md`에 정식 절차로 굳어져, 8일차 오전 실기
  전체(핸드오버 3회 포함)가 7.5fps로 돌았다 — "필수" 요구사항
  위반 상태였음. `usb_cam`(mjpeg2rgb)으로 30fps 전환 자체는 실측
  검증 완료(29.84Hz, GPU 15%). **[팀결정 — v83 회신] 그러나 즉시
  전환하지 않는다** — fps를 올리면 확정까지의 관측 시간이 줄어
  속도 추정이 나빠지므로(`_min_consecutive_frames`를 비례 조정
  하지 않으면 오히려 악화) 근거 없이 규정만 맞추는 건 무의미.
  **당분간 7.5fps 유지, 예측 오차를 실측한 뒤 30fps 전환 여부를
  결정한다**(다음 세션 순서 참고) — "몰라서 위반"이 아니라
  "측정해서 결정"으로 남긴다(과대 청구 금지/설명 가능성 원칙).
  캘리브레이션 완료:
  `fx=1427.5, fy=1429.8, cx=674.3, cy=359.2`, 왜곡계수(plumb_bob)
  `[0.0838,-0.0067,-0.0059,0.0080,0]`. depth 없어 평면교차가
  유일한 3D 산출 경로 — `belt_correction.py`로 벨트 평면 경험적
  보정 적용(RMS 4.5mm, 최대 8.6mm).
- 그리퍼: OnRobot RG2, 개폐폭 0~110mm, 파지력 3~40N(0.1N 단위).
  제어: `onrobot_rg_control`의 `OnRobotRGControllerServer` —
  `/onrobot/sendCommand`(`SetCommand`: `'o'`/`'c'`/`'i'`(+2.5N)/
  `'d'`(-2.5N)/숫자문자열(목표폭mm)), `/onrobot/pose`, 상태
  `/onrobot_joint_states`(리맵 필요, 아래 함정 참고).
- **pymodbus는 3.10 미만 필수**(`pip install --break-system-packages
  "pymodbus<3.10"`) — 3.10+는 `slave`→`device_id` 이름 변경으로
  `onrobot_rg_control`/corecode와 비호환.
- 컨베이어: 스테퍼+Microstep Driver(PUL/DIR, ENA는 절대 건드리지
  않음). 시리얼 9600 baud, `"<steps_per_sec>\n"`(직접 정의한
  프로토콜). 100 steps/sec 고정 사용. **[정정 — 9일차 저녁]
  "=2.0cm/s"는 틀렸다 — 발표에서 이 값을 말하지 말 것.**
  현재 확인된 값: **벨트 표면 약 5.0mm/s(스톱워치 실측),
  물체(비전) 약 2.9mm/s(3회 일관)** — 둘의 42% 차이 원인과
  2일차 값이 왜 4배 틀렸는지는 둘 다 미확정, 시연 이후 조사
  (경위는 실측기록.md 9일차 저녁분 참고). 탈조 없음은 여전히
  유효. 수동 제어: `~/bin/conveyor {on|off|rev|<steps_per_sec>}`
  (진단용 헬퍼, `conveyor_node` ROS2 구현체 아님 — 워치독 미구현).
- PC 구성 목표: 관제 PC(**GPU 탑재로 지정** — C270+YOLO12s 추론+
  컨베이어 제어) / 로봇 PC(RealSense+팔+그리퍼+음성+상태머신).
  **[현재 상태] 물리적으로 PC 1대뿐, 이 PC는 GPU 없음**(Intel
  UHD 620 내장그래픽만) — 2대 분리는 미집행, YOLO도 당장은 이
  CPU 전용 PC에서 진행.
- 분리수거 통 5개(로봇 양옆 배치): `paper_bin`/`plastic_bin`/
  `can_bin`/`battery_bin`/`review_bin`. 좌표: `config/
  bin_teach_points.py`. `review_bin`("확인 필요" 물리 라벨)은
  재질별(4통)과 처리상태별(1통) 분류가 섞인 의도된 비대칭 구성—
  재론하지 않음. 벨트 끝 일반쓰레기통은 미검출/미학습 물체 전용.
- **[정정 — 9일차] 클래스 6개(v6 모델 기준)**: `battery`(AA),
  `can`(무게측정 대상), `paper`(초코 종이상자 — "우유팩" 아님),
  `pet_labeled`, `plastic`(신규 — 페트병 형태가 아닌 뚜껑/용기류,
  무게측정 없이 바로 plastic_bin행), `plastic_bag`(묶인 것만,
  무게측정 없이 바로 review_bin행). **`pet_unlabeled`는 9일차에
  의도적으로 삭제됨**(오분류가 너무 많아 라벨/무라벨을 모두
  학습시킬 여유가 없었음, 사용자 확인) — 무라벨 페트병은 이제
  검출 자체가 안 되고 흘려보내짐(시연 물체는 라벨 있는 것만).
  **[팀결정 — v47, 배경] 클래스명은 항상 YOLO 모델 쪽에 문서·
  config를 맞춘다**(모델은 재학습 비용이 크고 쉽게 못 바꾸므로)
  — 이번 `pet_unlabeled`→삭제/`plastic`추가도 같은 원칙의 연장.
  **DB 규격 v2에 `pet_bottle_*`/`pet_unlabeled` 이름이 전달됐다면
  최신 클래스 목록으로 갱신 필요 — 팀 통해 DB 담당자에게 전달
  필요(미완, v47부터 이어지는 미완 항목).**
- 백업 경로: `/media/rokey/UBUNTU 24_0/재활용/backup` — USB
  상시 연결. 설계문서는 최신판(v32)만 로컬에 두고 이전 버전은
  이 경로로 이관(완료). USB는 vfat — 심볼릭 링크/실행권한/특수
  문자 파일명 불가, 유일한 사본으로 두지 말 것(GitHub+클라우드
  병행).

## 현재 확정값

| 품목 | 파지력 | close_wait | 파지 깊이(호버 기준) |
|---|---|---|---|
| can | **15N**(9일차 밤 10→15 상향, 찌그러짐 없음 확인) | 2.0s | 100mm |
| paper | (시연에서 완전 제외됨, 아래 "현재 단계" 참고) | — | — |
| battery(AA) | 기본값(~40N) | 1.0s | 110mm (Y=-264 고정) |
| pet_labeled | 기본값(~40N) | 1.0s | 100mm |
| plastic | **15N**(정정 — 10일차, 아래 참고) | 2.5s | 115mm |
| plastic_bag | 기본값(~40N) | 1.0s | 100mm(**확정 — 10일차, 관제PC gripper_profiles.yaml 실측, 기존 "[전제]" 해소**) |

`prediction_bias_mm`(품목별, 9일차 밤 클램프 참값 4.98mm/s
재검증 후 전부 불변 확정): can=0, battery=+30, plastic=+20.
칼만 vx 클램프 4.6~5.3mm/s(참값 기준, 전역 파라미터).

(전체 설정: `config/gripper_profiles.yaml`, `grasp_fail_joint_rad`는
`config/gripper_profiles.yaml` 별도 필드 — 아래 "알려진 함정"
`null`=40N 항목 참고)
`plastic` 추가 실측값: `prediction_bias_mm=+20.0`(**정정 — 9일차
저녁, 원래 40이었으나 깊이(108→115mm) 변경과 동시에 정해져
어느 쪽 효과인지 못 갈랐던 값. 깊이 확정 후 분리 재검증 —
41~43mm 이동량 중 40mm가 보정값이고 실제 예측 기여는 1~3mm뿐
(뚜껑 속도 0.2~0.35mm/s라 8초 앞을 봐도 미미), 40은 뚜껑
지름(~90mm) 대비 중심이 아니라 가장자리를 집는 과한 값이었음
— 20으로 낮춰 재시험, 실물 성공 확인**),
`grasp_fail_joint_rad=null`(찌그러진 뚜껑을 테두리로 물 때와
지름으로 물 때 관절각이 0.78rad 벌어져 판정 불가 — 원리적으로
plastic_bag과 같은 상황, "뚜껑은 여유가 커서 판정이 쉽다"던
이전 CLI 판단은 **온전한 뚜껑에서만 맞고 찌그러지면 틀림**,
정정).

**[정정 — 10일차, 이 문서 자체의 오독 발견] `plastic` 파지력은
10N이 아니라 15N이 최종 확정값이다 — 이 문서가 틀렸었다.**
`gripper_profiles.yaml` 주석은 시행착오 이력이 역순(위→아래로
과거)으로 쌓여 있는데, 그중 "40N→찌그러짐→10N으로(can과
동일하게) 맞춘다"는 **중간 단계** 기록을 이 문서가 최종값으로
오독해왔다. 실제로는 그 뒤에 **"10N으로는 이송 중 낙하"**가
추가로 관찰돼(이 낙하는 배치 성공으로 로그에 잡혀 조용히
묻힘 — 위 "로그만 보고 판단하지 말 것" 원칙과 같은 유형) 15N
으로 다시 올린 게 최종이었다(사용자 9일차 발언: "PLASTIC은
10N으로는 안될 거 같고 15N으로 설정해줘, 옮기는 과정에서
떨어지고 있어" — 커밋 `8fbe524` "plastic 15N 확정"). **10N(낙하)
과 40N(찌그러짐) 사이 여유가 좁으므로 이 값을 임의로 조정하지
말 것.** 위/아래 표의 "10N" 표기는 전부 이번에 15N으로
수정했음 — 과거에 이 문서를 근거로 10N을 쓴 판단이 있었다면
재검토 필요.

- 이동/하강 속도(채택값): 수평 vel=120/acc=40(HOME↔벨트 3.410s),
  수직 vel=100/acc=40(100mm 하강 2.194s). `config/motion_timing.yaml`.
- 사이클 시간(참고용 하한, 검출/좌표계산 시간 미포함): **20.8초**
  (품목 1개, 벨트→통→벨트 왕복).
- 좌표 정확도: `belt_correction` 적용 후 RMS 4.5mm, 최대 8.6mm
  (x=312.9 지점 재검증 필요 — 국소 이상치 의심, 미해결).
- 관절 가동 범위(물리 한계보다 좁게 설정): J2 ±95°, J3 ±135°,
  J5 ±135°. J1/J4/J6 제한 없음.
- TCP 힘 한계 160N(일반 모드). SAFE_OFF 원인은 확정 못함(원인
  규명 시도 종료) — 검증 기준으로 대체: 저강성(500)+당김 방향+
  핸드오버 자세+라벨 페트병으로 10회 연속 SAFE_OFF 0회.

## 알려진 함정

- **[실측 — 10일차] `recycle_detection` 노드의 `device` 파라미터를
  셸에서 `-p device:="0"`로 주면 ROS2가 이를 INTEGER로 해석해
  노드가 죽는다.** 반드시 `-p device:=\"0\"`처럼 이스케이프해
  문자열로 전달할 것(`device:="cpu"`는 문자열이라 문제 없음 —
  숫자로만 보이는 값일 때 발생).
- **[팀결정 — v83 회신] 임시 우회를 문서에 남길 때는 반드시
  다음 세 가지를 함께 적을 것: (1) 무엇을 우회했는가, (2) 원래
  요구사항이 무엇인가, (3) 언제 재검토해야 하는가.** [실측 —
  8일차] `v4l2_camera`가 MJPG를 못 다뤄 급하게 YUYV(7.5fps)로
  우회한 게 `readme2.md`에 그냥 "터미널1 실행 명령"으로만 적히면서
  정식 절차로 굳었다 — CLAUDE.md의 "C270 30fps 필수"와 대놓고
  모순인 채 8일 동안 아무도 재검토하지 않았다(그 상태로 실기
  운전 전체가 돌았음). 위 세 가지가 없으면 우회가 요구사항을
  조용히 대체해버린다.
- **`pip install -r team_share_yolo12s/requirements.txt`가 `numpy`를
  2.x로 올려 `cv_bridge`를 깨뜨린다**(cv_bridge는 numpy 1.x 기준
  컴파일됨) — `ultralytics`가 최신 `opencv-python`을 끌어오는데
  그게 `numpy>=2`를 요구하는 게 원인. **해결: pip `opencv-python`
  패키지를 제거하고 시스템 `python3-opencv`(4.6.0, apt) +
  `numpy<2`를 쓸 것** — ultralytics 동작엔 지장 없음(실측 확인).
  재설치할 일이 있으면 이 순서를 반복할 것.
- 그리퍼 `'c'`(닫기)는 **비동기** — 반환돼도 물리 동작 안 끝남.
  닫기 후 품목별 `close_wait_sec` 그대로 대기할 것(위 표 참고).
  **[실측 — 9일차 저녁] 이 원칙이 실제로 유용했던 사례**:
  `_call_gripper`의 서비스응답 대기(`timeout_sec`)가 60회 중
  4회 8초를 꽉 채움(원인 미상[불명] — Modbus 폴링 지연/응답
  유실 추정) — 무정지 상태에서 그동안 물체가 약 23mm 끌림
  (인터록 하에서는 벨트가 서 있어 안 드러났던 문제, 백로그
  버그와 같은 유형). **8.0→2.0초로 하향 적용**(관제PC 제안,
  CLI 승인) — `close_wait_sec` sleep이 응답 타이밍과 무관하게
  물리적 완료를 보장하므로 타임아웃 값을 줄여도 파지 완료
  보장에는 영향 없음(위 원칙의 직접 근거), 4회 전부 결국 파지
  성공했다는 것도 "명령은 실행됐고 응답만 지연"을 뒷받침. 근본
  원인(응답 지연 자체) 조사는 시연 이후 과제.
- **파지력은 세션 내내 유지되고 자동 리셋 안 됨.** 매 파지 직전
  `gripper_profiles.yaml` 값을 명시적으로 설정할 것 — 이전 값을
  가정하지 말 것.
- **[실측 — 9일차 저녁] `gripper_profiles.yaml`에서 `force_n: null`
  은 "설정 안 함"이 아니라 "기본값 40N 적용"이다.** v5 시절
  `pet_unlabeled`(뚜껑)의 `force_n`이 null이라 기본 40N이 걸려
  **뚜껑이 실제로 찌그러졌다**(사고로 발견). 새 품목 추가 시
  null을 "안전하게 약하게"로 착각하지 말 것 — 힘이 걱정되면
  명시적으로 낮은 값을 넣을 것.
- **[실측 — 9일차 저녁, 3회 재발] `colcon build`는 워크스페이스
  루트에서 실행할 것, 패키지 디렉터리 안에서 하면 안 됨** —
  `install/`이 심볼릭 링크가 아니라 복사본이라 반영이 안 되고,
  패키지 디렉터리 안에서 빌드하면 조용히 설치본이 안 갱신된
  채로 노드가 뜬다(검증이 실제 실행경로를 재현하는지 확인하라는
  원칙의 또 다른 사례). **기록해도 같은 날 세 번 재발함** —
  관제PC가 "빌드 후 설치본 값을 매번 출력해 확인"을 습관화
  하기로 함(단순 기억이 아니라 매 빌드 뒤 확인 단계를 고정할 것).
- **[실측 — 9일차 저녁] 파지 사이클 진행 여부는 `/ui/state`의
  `stage`로 확인할 것, 로그의 "holding" 문자열로 판단하지 말 것**
  — 그 라인은 파지 중에는 안 찍혀서, 로그만 보고 "안 잡고 있다"고
  오판해 파지 사이클 도중 노드를 내린 사고가 있었다.
- **[실측 — 9일차 저녁] 파지 실패(명시적 `False`) 시 그리퍼를
  열지 말 것.** 판정 자체가 틀릴 수 있는데(교차클래스 오분류로
  엉뚱한 임계값 적용) 열어버리면 실제로 들고 있던 물체를 그
  자리에 떨어뜨린다(종이박스로 실측 재현). 실패 시엔 그냥 들고
  복귀만 하고, 다음 파지 직전의 기존 개방 동작에 맡길 것.
- `MoveLine`은 시작·끝점이 각각 도달 가능해도 그 사이 직선 경로가
  관절 한계에 걸리면 **`success=True`를 반환하면서 안 움직인다.**
  `get_current_posx()`로 실제 이동 여부를 항상 확인할 것. 자세가
  크게 바뀌는 이동은 중간 경유점으로 나눌 것.
- 통 PLACE(낮은 z)에서 상승 없이 바로 다음 지점으로 이동 금지 —
  반드시 "수직 상승 → 수평 이동 → 수직 하강" 3단계 시퀀스.
- 재시도 시 하강량은 항상 **호버 기준 절대값**으로 계산 — 이전
  실패 시도에 상대적으로 값을 더하지 말 것.
- 도달 불가 지점에서 자세 스캔 시 `ry`뿐 아니라 **`rz`도 함께**
  바꿔볼 것 — 안 그러면 실제로 존재하는 해를 놓칠 수 있음.
- 그리퍼 노드가 완전히 죽으면(`ros2 node list`에 안 보임) Compute
  Box 전원 재기동 필요. 수동 재실행 시 **`-r /joint_states:=
  /onrobot_joint_states` 리맵 필수**(빠지면 조용히 실패).
  `/onrobot/restartPower` 서비스는 호출 금지(즉시 노드 크래시).
- 로봇 스택 재기동: `ros2 launch m0609_rg2_bringup bringup.launch.py
  mode:=real host:=192.168.1.100 model:=m0609`(그리퍼 노드 포함됨).
- "펜던트 화면 정상"과 ROS2 상태 조회가 다르면 펜던트를 신뢰—
  ROS2 쪽은 스택 재기동 전까지 stale할 수 있음.
- **[실측 — 8일차] `joint_states`는 100Hz로 정상 발행되는데
  `get_current_posx`만 무응답인 증상 조합은 "펜던트 수동모드
  전환"과 "옛 bringup 프로세스가 제어권을 붙든 채로 하드웨어만
  재연결됨" 둘을 구별 못 한다.** 하드웨어(로봇/캠/벨트) 재연결
  후 이 증상이 보이면 펜던트 상태부터 확인하지 말고 bringup을
  재기동해볼 것 — 재기동으로 해결되면 후자였던 것.
- **[실측 — 8일차] 아두이노(컨베이어)가 리부트/재연결 후 한 번에
  안 잡힐 수 있음** — `lsusb`에도 안 보이고 커널 로그에도 이벤트가
  없다가, USB를 다시 뽑았다 꽂으니 잡힘(접촉 문제로 추정[추론]).
  **시연 전 체크리스트에 "아두이노 lsusb 확인" 추가할 것.**
- **[실측 — 9일차 저녁, 저장소 결함, 수정·커밋 완료] `config/
  tracking.yaml`의 최상위 키가 `tracking_node`인데 실제 노드
  이름은 `tracking_pick_node`라 이 설정파일이 로드된 적이
  없었다** — 기동 시 `calibration ready=False`로 조용히 막혀
  (로봇이 좌표 계산 단계에서 정지, 안 움직임) 발견됨. 저장소
  원본 파일에 `tracking_pick_node:` 키로 수정·커밋 확인됨(커밋
  `12b08a5`).
- **[실측 — 9일차 밤, 위험] USB 장치를 다시 꽂으면(포트 이동
  포함) 그 장치를 쓰는 노드를 반드시 재기동할 것 — 안 하면
  조용히 먹통이 된다.** 리얼센스를 다른 USB 포트로 옮기는 과정
  에서 아두이노(컨베이어)가 재열거되며 장치경로가 바뀜(`ttyACM0`
  →`ttyACM1`) — `conveyor_node`는 이미 열어둔 옛 핸들을 계속
  붙들고 있어 STOP 명령이 하드웨어에 전달 안 됐는데, **로그에는
  "컨베이어 정지"라고 정상 출력되면서 실제로는 벨트가 계속
  돌았다.** `by-id` 경로는 노드가 **핸들을 새로 열 때만** 효과가
  있고, 이미 연 핸들은 장치가 바뀌어도 안 따라간다. 그리퍼가
  죽으면 눈에 띄게 티가 나는데(응답 없음/에러) 이건 조용히
  실패해서 더 위험함 — **USB 재연결 시 관련 노드(특히
  `conveyor_node`) 재기동을 습관화할 것.**
- **[재발견·정정 — 9일차 저녁] 관절 특이점 회피(알람레벨1)가
  battery_bin 전용이 아니라 여러 동작(벨트복귀/각 통 상공이동·
  승강/파지승강/실패복귀)에서 고르게 36회 발생함** — 5일차에
  battery_bin만 좁게 조사하고 "재현 안 됨, 기각"으로 덮었던
  판정이 조사범위 부족이었던 것으로 정정됨. 컨트롤러가 자동
  회피 중이며 정지·오류 없이 전부 배치 성공 — 기능적 실패는
  아니고 시연 화면에 관절이 튀는 것처럼 보이는 시각적 문제.
  **원인(어떤 자세조합이 특이점 근처를 지나는지) 조사·티치포인트
  재설계는 시연 이후 과제** — 검증된 자세를 지금 바꾸면 오늘
  쌓은 데이터가 무효화됨. 상세는 실측기록.md 9일차 저녁분 참고.
- **`reset_workpiece_weight` 서비스 호출 금지.** [실측 — 6일차]
  이 서비스를 호출한 뒤 `get_workpiece_weight`뿐 아니라 무관한
  `get_robot_state`까지 ROS2 서비스 전체가 타임아웃되는 사고 발생
  (로봇 자체는 펜던트로 확인 시 정상, ROS2만 stale — 위 항목과
  같은 패턴). 로봇 스택 재기동으로만 복구됨. 인과관계는 규명 못
  했지만 리스크 대비 실익이 낮아 사용 금지 — baseline 차분(파지
  전후 `get_workpiece_weight` 두 번 호출) 방식으로 이미 정확하게
  동작하므로 이 서비스가 필요하지 않다.
- **순응제어(`task_compliance_ctrl`) 진입 직후 변위 기준점을 잡을
  때, 진입 *이전* 위치를 기준으로 삼지 말 것.** [실측 — 6일차,
  핸드오버 오탐 릴리즈 사고] 저강성으로 전환하는 순간 그 자체가
  (물체를 들고 있다면) 중력으로 인한 처짐을 만들어내고, 이걸 기준
  이전 위치와 비교하면 "당김"으로 오인해 사람 없이 그리퍼가 열림
  (물체 낙하 사고 재현, 사진으로 확인). **원칙: 기준점은 반드시
  순응모드 진입 + settle 대기 *이후*에 캡처할 것** — 중력 처짐은
  이미 기준점에 포함되고, 그 이후의 "추가" 변위만 실제 외력(당김)
  으로 잡힌다. 물체를 든 상태에서 순응 제어를 켜는 모든 경우에
  적용되는 원리.
  **[재발 — 8일차, 관제PC 실물 테스트]** 위 수정(고정
  `compliance_transition_sec`=0.5초 대기 후 기준점 캡처)만으로는
  부족했다 — 사람 없이 약 3초 만에 릴리즈되어 라벨 페트병 낙하
  재현(로그 미확보, 재현 상황으로 추정). 1차 재수정으로 "위치가
  실제로 멈출 때까지(적응형 settle) 대기 후 캡처"를 시도했으나,
  **이것도 근본 해결이 아니었다** — "연속 폴링 간 이동량이 임계값
  이하"는 변화율 조건일 뿐 총량 조건이 아니라서, 물체가 매 폴링
  조금씩(임계값 이하로) 계속 처지는 **느린 크리프**는 이 조건을
  계속 통과하면서도 몇 초에 걸쳐 누적되면 결국 총 변위 임계값을
  넘는다 — 기준점을 한 번 잡고 "누적 변위(총량)"만 재는 구조인
  한 settle을 아무리 정교히 해도 이 문제가 남는다.
  **[정정 — 8일차, 판별 방식 자체를 전환]** "누적 변위(총량)"
  기준을 버리고 **"수평 이동 속도"** 기준으로 바꿨다 — 사람이
  당기는 동작(빠름, 추정 20~50mm/s)과 중력 처짐/크리프(느림,
  6일차 사고 실측 평균 약 3.7mm/s)는 총량으로는 안 갈려도 속도로는
  뚜렷이 갈린다는 원리. 릴리즈 조건 = (수평 속도가 연속 2회 이상
  `pull_speed_mm_per_s`=15.0mm/s 이상) **AND** (시작점 대비 누적
  3D 변위가 `release_threshold_mm`=10mm 이상, 미세 흔들림 배제용
  하한선으로 유지) — `_handoff_item` 참고. 이 방식은 적응형
  settle 자체를 불필요하게 만들어 제거했다(속도 게이팅이 스스로
  느린 크리프를 걸러내므로). [전제, 미검증] `pull_speed_mm_per_s`
  =15.0은 실측값과 추정 상한 사이 중간값 — 로봇 실물로 처짐/당김
  두 조건의 속도 분포를 재본 뒤 조정할 것(매 폴링 상세 로그를
  남기도록 함). 다음 순서 참고.
- **예측 시간 관리 원칙(`extra_gap`).** [실측 — 6일차, 무게측정
  baseline 타이밍 버그] `predict_ahead_sec()`로 계산한 예측 시간에
  `extra_gap`(=지금 - 마지막 검출 시각)을 더해 "지금 이 순간"
  기준으로 목표를 재계산하는 게 `_execute_pick`의 핵심 설계인데,
  **예측 계산 시점 이후·파지 완료 이전에 실행되는 새 동작을 추가할
  때마다 그 소요 시간이 `extra_gap`에 반영되는지 반드시 확인할
  것.** 반영 안 하면 예측이 그만큼 뒤처져 물체의 트레일링 엣지만
  잡거나 완전히 놓친다(실측 재현: baseline 무게 평균측정을 예측
  계산 이후에 배치했다가 extra_gap=6.5초, total_ahead=13.4초까지
  누락돼 하부만 잡는 실패 발생 → baseline을 예측 계산 이전으로
  옮겨 수정). 현재 `extra_gap`에 포함되는 동작: 그리퍼 열기, 힘
  정규화, baseline 무게 측정(weight_check 품목만) — 이후 새 동작을
  추가할 때 이 목록도 함께 갱신할 것.
- **[실측 — 10일차, 백업 중 발견] 저장소/패키지 안에 든 모델
  가중치 파일명이 같아도 버전이 다를 수 있다.** `object_detection`
  패키지에 번들된 `recycle_best.pt`가 md5 `16fe1922`로 **v5(구버전,
  실사용 안 함)** 였음 — 실제 운용 모델은 v6(`8b4d25fa`)이고
  컨테이너가 별도 경로(`/home/rokey/Rocycle_Project/models/
  recycle_best.pt`)를 마운트해서 씀. 같은 파일명만 보고 백업/배포
  하면 구버전을 정본으로 착각할 위험 — **파일명이 아니라 md5로
  확인할 것.** v5 가중치는 `models/v5/`에 별도 보관, 백업에서는
  제외(25MB→76KB로 정리).

- **[실측 — 10일차, v172 조사 중 발견] `tracking.yaml`의
  `vision.dry_run` 기본값이 `true`다.** `tracking_pick_node` 실행
  시 `-p vision.dry_run:=false`를 명시하지 않으면 파지 없이
  추적만 하고 벨트만 돈다 — 오늘 이걸 빠뜨려 벨트만 30초 도는
  경미한 사고가 있었음. **기동 명령에 반드시 `dry_run:=false`
  포함할 것**(정상 운용 시).
- **[실측 — 10일차] 패키지 이름이 파일마다 다르다 — 혼동 주의.**
  `conveyor_node`/`tts_node`는 `recycle_robot` 패키지
  (`ros2 run recycle_robot conveyor_node`), `tracking_pick_node`는
  `rocycle_robot` 패키지(`ros2 run rocycle_robot
  tracking_pick_node`) — **이름이 한 글자 차이(`re`/`ro`)라
  기동 명령에서 틀리기 쉬움.**
- **[실측 — 10일차] `config/vision.yaml`은 저장소에만 있고
  관제PC 실행 트리(`install/`)엔 없다 — 실제 운용에서 쓰이지
  않는 죽은 설정파일이다.** 문서·설정 참고 시 이 파일을 "현재
  쓰이는 설정"으로 인용하지 말 것.

## 설계 원칙 (위반 시 즉시 지적할 것)

1. **크기로 클래스를 구분하지 않는다.** eye-in-hand top-down에서
   픽셀 크기는 카메라-대상 거리에 의존적이며, RealSense depth
   오차(±1~2mm)로는 보정 불가. 실루엣 1순위, 색상 2순위.
2. **OCR에 의존하지 않는다.** 텍스트 판독이 필요하면 DB 조회로
   우회한다.
3. **외부화 원칙.** 클래스 이름과 물리 수치는 코드에 하드코딩하지
   않고 YAML로 분리한다. 판정 로직에 품목명에 대한 분기를 두지
   않는다 — `clarification_rules[material_type].trigger`처럼
   설정을 조회해서 동작해야 한다.
4. **페이로드 6kg 제약** 내에서 대상을 선정한다.
5. **재현성 우선.** 기능 추가 여부는 "5일 안에 10회 시연해서
   10회 성공 가능한가"로 판단한다.
6. **과대 청구 금지.** 구현한 것 이상으로 표현하지 않는다.
   예: 규칙 덮어쓰기를 "학습"이라 부르지 않는다.
7. **판정을 바꾸는가를 먼저 묻는다.** 새 기능을 검토할 때
   "구현 가능한가"보다 "이 기능이 판정이나 동작을 실제로
   바꾸는가"를 먼저 확인한다. 판정 결과가 같다면 구현
   난이도와 무관하게 제외 대상이다.

## 문서 작성 규칙

- [원문]/[추론]/[전제] 구분 표기를 항상 유지한다.
- 저장소 파일을 인용할 때는 경로와 행 번호를 함께 밝힌다.
- 확인하지 않은 것을 확인한 것처럼 쓰지 않는다.
- **[원문]으로 인용할 때는 값 자체뿐 아니라 그 값이 어떤 맥락에서
  나온 것인지도 함께 확인한다.** 숫자가 문서에 존재한다는 것과
  그 숫자가 우리가 쓰려는 의미인 것은 다르다.
- **[팀결정 — v80 회신, 재발 방지] 검증 도구가 실제 실행 경로를
  그대로 재현하는지 확인할 것.** [실측 — 8일차] curl로 `topic=
  /image_raw`(인코딩 안 함)를 쳐서 "정상"이라 결론 냈는데, 실제
  브라우저는 `encodeURIComponent`를 거쳐 `%2Fimage_raw`를 보내고
  있었다 — 검증에 쓴 요청과 실제 코드가 보내는 요청이 달라서
  원인을 오판(자원 소진 탓으로 잘못 결론)했다. **같은 유형이 이전에
  캘리브레이션 단위테스트(임의 값으로 통과시켰다가 실측 157mm
  오차)에서도 있었다** — 두 번째 재발. "테스트가 통과한다"와
  "실제 실행 경로와 같은 입력으로 테스트했다"는 다르다는 걸
  항상 확인할 것. **[팀결정 — v103 회신, 세 번째 재발 후 절차화]
  세 번째 재발**(6일차 "정지 상태 힘센서 드리프트 ±38g" 실험이
  실제 baseline 조건(다른 자세+벨트 가동 중)을 재현 안 해 "센서
  노이즈"로 오진단, 8일차 재검증으로 철회). **기록해두는 것만
  으론 재발을 못 막는다는 뜻 — 실측 계획을 세울 때 "이 측정이
  실제 실행 경로의 어느 조건을 재현하고 어느 조건을 재현하지
  않는가"를 사전에 명시적으로 적을 것**(사후 점검이 아니라
  계획 단계의 필수 항목으로). **[팀결정 — v110 회신, 네 번째
  재발 후 착수 조건으로 격상] 8일차에만 네 번째** — 관제PC
  헤드리스 검증이 1600×900으로 이뤄졌는데 실모니터는 1920×1080
  이었다(위 UI 항목 참고). 이번엔 "재현 안 한 조건으로 오판"을
  넘어 **"존재하지 않는 문제를 고치느라 시간을 썼다"**(손목캠/
  조작칸 높이를 여러 차례 조정 — 실해상도에선 애초에 다 들어감).
  기록만으로 안 잡히므로 **작업 착수 전 한 줄 확인을 의무화**:
  ```
  실측·검증을 시작하기 전에 한 줄로 적을 것:
    이 측정의 조건이 실제 운용 조건과 같은가?
    다르다면 어디가 다른가?
  ```
  사후 점검이 아니라 **착수 조건**으로 두면 네 번째 유형이 다시
  안 나온다는 게 팀 판단.
  **[팀결정 — v116 회신, 자매 규칙으로 묶음] 위 규칙과 "관측이
  측정 방식 자체의 산물은 아닌가"는 같은 계열의 자기점검 질문
  이다** — 하나는 "검증이 실제 실행 경로를 재현하는가"(입력 쪽
  오류), 다른 하나는 "이 관측값이 측정/판정 메커니즘 자체가
  만들어낸 인공물은 아닌가"(출력 해석 쪽 오류). **[실측 사례
  — 8일차] 핸드오버 변위 임계값 논의에서 "여유%가 12%→6%→3%로
  준다"를 위험 추세로 오독**(CLI 본인이 이 실수를 함) — 릴리즈
  조건이 "변위≥임계값을 넘는 순간 멈춘다"인 이상 관측값은
  정의상 항상 임계값 바로 위에서 잡히므로, 이 지표로 임계값을
  계속 낮추면 끝없이 내려간다(v96에서 overshoot에 적용했던
  같은 원리를 이번엔 "여유% 추세"에 적용 못 함 — 원리를 안다고
  매번 알아채는 게 아니라는 뜻). **두 규칙을 함께 기억할 것**:
  실측 계획 시작 전 "조건이 같은가"뿐 아니라 "이 관측이 메커니즘
  자체의 산물은 아닌가"도 같이 물을 것.
  **[팀결정 — v138 회신, 세 번째 자매 규칙] "구성요소가 각각
  검증됨"과 "그것들을 이은 경로가 검증됨"은 다르다.** [실측
  사례 — 9일차, 두 번 재현] (1) 무게 게이트: 무게측정 정확도
  (v117)·측정 위치(v116)·review_bin 배치(v112)가 각각 검증됐다고
  "선행조건 다 해소"로 결론 냈다가(웹 클로드 본인이 이 실수를
  하고 스스로 정정) "셋을 잇는 전체 흐름 자체는 한 번도 실행된
  적 없는 새 코드"라는 걸 놓칠 뻔함. (2) 파지실패감지: 코드
  구현이 끝났다고 봤는데 첫 실사이클에서 executor 블로킹 버그가
  바로 나옴("1단계는 로그만" 설계 덕에 안전하게 잡힘, 위 항목
  참고). **세 번째 자기점검 질문으로 추가**: "부품들이 각각
  맞다는 것과, 그 부품들을 이은 새 경로 자체가 처음 실행된다는
  것을 구분했는가" — 새로 이어붙인 통합 경로는 항상 "처음
  실행되는 순간"으로 취급하고 별도로 검증 시간을 확보할 것.
- 상세 실측 경위·정정·철회 이력은 `실측기록.md`에 시간순으로
  남긴다 — 무효화된 결정도 왜 바뀌었는지 지우지 말고 보존한다
  ("설명 가능성" 평가의 근거가 된다).
- **[팀결정 — v53 회신, 재발 방지] 일과 종료 시 그날 작업 로그를
  `실측기록.md`로 이관한다.** `CLAUDE.md`에는 결론(현재 유효한
  확정값·원칙·함정·다음 순서)만 남긴다 — "현재 단계"가 시간순
  로그로 다시 누적되지 않게 매일 정리할 것(v46에서 한 번, v53에서
  또 한 번 200줄 안팎까지 정리했는데 둘 다 며칠 새 500줄 이상
  불어났던 전례가 있음). 옮길 때 정보를 지우지 말고 그대로 이관할
  것 — 실측기록.md가 최종 산출물(보고서)의 원재료다.
- **[팀결정 — v63 회신, 재발 방지] 세션 종료 시(이더넷 연결 시점)
  `rocycle` 원격 `main`에 커밋·푸시한다.** `ros2/rocycle_robot/`가
  8일간(9월 5일 한 번의 커밋 이후) 한 번도 git에 안 올라가고
  로컬 디스크 하나가 유일한 사본이었던 적이 있었다(6일차 발견) —
  디스크 손상 시 전부 유실될 뻔한 상황. 실측기록 이관과 같은
  유형의 문제(습관화 안 하면 반복됨)라 같은 방식으로 규칙화한다:
  ```
  카메라·벨트 연결 -> 검증·수정 (로봇 PC)
  이더넷 연결 -> git add/commit/push to rocycle main
  -> 관제 PC pull -> 확인
  -> 다시 카메라·벨트 연결
  ```
  **관제 PC는 항상 `main`만 pull한다** — 기능/노드별 브랜치는
  이력 보존이 필요할 때만 임시로 만들고, 작업이 끝나면 `main`에
  병합 후 삭제한다(여러 브랜치가 동시에 살아있으면 어느 조합이
  동작하는 상태인지 헷갈리고 병합에 시간을 쓰게 됨 — 6일차 실측
  경험).

## 안전

- 힘 임계값은 스쿠핑 프로젝트 값(±20N)을 그대로 쓰지 않는다.
  구조(감지→즉시 해제→HOLD)만 재사용하고 수치는 실측한다.
- 안정화 확인(투입 감지, PICK 직전 재확인)은 보조 수단이며,
  최종 안전망은 힘 하드리밋(HOLD)이다.
- 순응 핸드오버는 힘 기반보다 **위치(TCP 변위) 기반 감지**를
  우선한다 — 관절 엔코더가 힘 센서보다 정밀하고 한계값(관절
  3°≈TCP 약 26mm)이 명시돼 있다.
- **SAFE_OFF 자동 복구는 설계 목표가 아니다** — "발생 자체를
  막는다"가 목표, 발생하면 사람이 복구한다(API 복구는 신뢰 불가
  확인됨, 티치펜던트에서 오류 팝업 닫고 서보 켜기로 복구).
- 관절 가동 범위·TCP 힘 한계는 "현재 확정값" 표 참고.
- 벨트/통 등 물리 구조물은 "면"이 아니라 "입체"로 취급 — 접근
  높이를 애매하게 계산하면 옆에서 눌러버릴 수 있다.
- **[정정 — 10일차, 문서가 실제와 어긋나 있었음] 컨베이어는
  현재도 파지 사이클마다 정지한다(`stop_during_pick: true`) —
  "무정지 최종 확정"은 틀린 기록이었다.** v139 설계는 1단계
  (인터록 유지, true)→2단계(false로 무정지 실물시험)→3단계
  순으로 계획됐고, 2단계 무정지 시험 자체는 실물로 진행됐지만
  **벨트 진동이 계량에 실려 빈 캔이 net 106.2g으로 측정되는
  문제(실제 약 16g)가 나와, false를 영구 설정으로 반영하는
  작업이 끝나지 않은 채 방치됐다** — `tracking.yaml`엔 이
  항목 자체가 없고, 코드 기본값(`declare_parameter(...,
  True)`)이 그대로 살아있던 것(10일차 관제PC 실측: `ros2 param
  get`도 True, 코드 227행 주석도 "문제있으면 true로 되돌린다"
  로 되어있어 true가 임시가 아니라 사실상 최종 상태로 굳음).
  **[운영 결정 — 10일차] 촬영 임박으로 되돌리지 않고 현재
  상태(true) 유지, 문서만 정정.** 오늘 완주 여러 회차가 전부
  이 상태로 검증됨 — 무정지 재시도는 계량 중 정지 유지(1~2초
  등) 같은 별도 완화책을 먼저 구현·검증한 뒤 시연 이후 과제로.
  **도달한계 안전정지**(가장 앞선 미처리 물체가 `max_reachable_x`
  근접 시 정지, 처리되면 재가동) 기능 자체는 구현·검증 완료
  상태로 남아 있고 인터록과 별도로 계속 작동한다 — v48이 막던
  "다음 물체가 처리 중 도달범위를 벗어나는 문제"(6일차, NOT
  REACHABLE)에 대한 추가 안전장치로 유효. 정지 사유는 집합으로
  관리(인터록/도달한계/계량, `tracking_node` 내부), 비상정지는
  집합 밖 별도 하드상태, 전역 워치독 120초. 상세는 "현재 단계"
  절·실측기록.md 참고.
- **[구현·실물검증 완료] `weight_check`가 `can`에만 정의돼 있어
  물체가 오분류되면 원칙7(비전으로 못 보는 걸 힘으로 본다)이
  우회되는 구조였다** — 진짜 원인은 `_place_item()`의
  `human_handoff` 분기가 무게초과 판정(`forced_bin`)보다 먼저
  return하는 순서 버그였음(수정, 커밋 4e8be69). **최종 상태**:
  `can`/`pet_labeled` 둘 다 `weight_check` 활성화, 임계값
  0.20kg로 통일(오분류가 나도 결과가 안 바뀌도록 — 다만 이게
  "분류 정확도가 안 중요하다"는 뜻은 아님, 화면표시·그립력은
  여전히 분류결과에 좌우됨). baseline은 중앙값, `net_min_kg`
  =-0.15kg(계측신뢰불가 하한, 로드셀 27초주기 진동 포락선
  ±125g 바로 바깥). **분류 결과를 안전 게이트로 쓰지 말 것**
  원칙은 그대로 유효.

## 현재 단계

**[팀결정, 반드시 먼저 읽을 것] 관제PC 실제 운영 파일은
`tracking_pick_node.py`다, `tracking_node.py`가 아니다.** 후자는
개발PC(GPU/Docker 없음) dry-run 테스트 + 안전 로직 최초 설계용
원본 — **여기만 고치고 끝내면 시연에 반영 안 된다.** 애매하면
관제PC에 실제 파일 확인을 먼저 요청할 것. 검출은
`tracking_pick_node.py`가 아니라 별도 Docker
`object_detection/recycle_detection.py`가 전담하고 tracking은
그 결과(JSON)를 구독만 함.

**[정정 — 10일차, v172 조사] "GitHub `Rocycle` 저장소 Final
브랜치가 운영코드"라던 v169 결론은 낡은 것이었다** — 관제PC가
v63 프로토콜대로 정기 커밋하는 대상은 `Rocycle` 저장소 **main**
브랜치이고, 오늘 커밋(82d0fae)도 거기 들어갔다. **현재 정확한
이해: `Rocycle` 저장소 main = 운영코드(최신), Final 브랜치 =
DB브릿지/웹API 전담(UI 쪽) — Final의 `tracking_pick_node.py`는
구버전 스냅샷이라 운영코드로 참고하지 말 것.** (이 문서
`ROKEY-Team-Project` 저장소의 main과는 별개의 저장소이니 혼동
주의 — `Rocycle`은 GitHub `Taegyu-Lee1117/Rocycle`.)

- 주제: **컨베이어 기반 재활용 분류 협동로봇**(벨트 이송 + 동적
  추적 + 예측 파지). **[팀결정] 강사 승인 완료, 구현 단계.**
- **완료·실물검증된 것**: YOLO 실시간 검출(v6), 벨트 위 이동
  물체 파지(can/battery/pet_labeled/plastic/plastic_bag — paper
  제외, 아래 참고), 무게게이트(B, can+pet_labeled 통일임계값
  0.20kg), 그리퍼 위치기반 파지실패감지 1단계(can/paper/battery
  실사이클 검증 완료), 라벨 페트병 순응제어 핸드오버(7/7 성공),
  순차 처리 인덱싱, 음성 게이팅, 컨베이어 노드 분리, 관제 UI,
  can measure_pose, **도달한계 안전정지**(무정지 자체는 미적용
  상태로 확인됨 — `stop_during_pick: true`, 아래 안전 절 참고),
  (클램프 4.6~5.3mm/s 확정, 7종 재검증 파지실패 0건), 리얼센스
  파지자세검증 1단계(로그전용, 실물검증 완료).

### [배포 확정] v6 YOLO 모델 + 최종 그리퍼값
`pet_unlabeled` 삭제, `plastic`(뚜껑/비닐용기류) 신규.
`item_routing.yaml`/`gripper_profiles.yaml`에 `plastic` 항목
반영 완료. 롤백 시 `pet_unlabeled: {type: bin, bin: plastic_bin}`
그대로 남겨둠(롤백 안전 확보).
```
품목          파지력   close_wait  깊이(호버기준)   grasp_fail_joint_rad
can          15N     2.0s       100mm           0.4638
paper        (미사용)
battery      기본40N  1.0s       110mm           0.6864
pet_labeled  기본40N  1.0s       100mm           null(종이오분류 대응)
plastic      15N     2.5s       115mm           null(원리적 불가)
plastic_bag  기본40N  1.0s       100mm(확정, 10일차)  null(원리적 불가)
```
`plastic`의 `prediction_bias_mm=+20`(40에서 정정, 깊이변경과
분리 재검증 완료). `can` 파지력 10→15N 상향, 찌그러짐 없음
확인(사용자 육안).

### [완료] 무게 게이트(B) — 최종 상태
근본원인은 `_place_item()`의 `human_handoff` 분기가 `forced_bin`
보다 먼저 return하던 순서버그(수정 완료, 커밋 4e8be69).
```
can          weight_check 활성, threshold 0.20kg
pet_labeled  weight_check 활성, threshold 0.20kg (can과 통일 —
             오분류가 나도 결과가 안 바뀌게 하는 설계, 3-2절)
공통         baseline 중앙값, net_min_kg=-0.15kg(계측신뢰불가
             하한), 파지폭 교차검증은 human_handoff 라우팅
             품목만(can 자동제외)
```
**주의**: 임계값 통일은 안전 이중장치일 뿐 "분류가 부정확해도
된다"는 뜻이 아님 — 그립력·화면표시는 여전히 분류 정확도에
좌우됨. **운영규칙**: 시연 소품은 "비었거나(<50g) 확실히
무겁거나(>350g)"만 사용, net 150~250g대는 판정불가 — 8일차
"내용물 든 캔"(net 235g) 재현은 절대 금지.

**[운영규칙, 반드시 지킬 것] 로봇을 홈 자세로 보내고 시작하지
말 것 — baseline이 팔 현재 자세에서 측정됨.** [실측 — 9일차
밤] 홈 자세(367.3,7.0,424.3)에서 baseline을 재면 1143.6g로
다른 회차(1288~1382g)보다 183g 낮게 나옴 — **[정정 — 10일차
아침, 관제PC] baseline이 낮으면 net(=postpick-baseline)은
오히려 커진다(위로 밀림)**, 빈 캔이면 net이 임계값(200g)에
바짝 붙는 방향(196g대) — 제가 처음에 "관절각 차이로 net이
음수로 어긋난다"고 잘못 설명했음, 정정함. **이 홈자세 성분의
정확한 기전은 아직 미규명**(관절각 때문이라고 단정하지 말 것)
— 로드셀 27초 주기 진동(아래 항목)과는 별개의, 아직 설명 안
된 잔차. **결론(운영규칙)은 그대로 유효**: 사이클 시작 전
로봇을 벨트 상공(약 300,-264,430)에 둔 채로 시작할 것, 홈으로
보내지 말 것 — PC/노드 재기동 후에도 로봇 실제 자세를
`get_current_posx`로 먼저 확인하고 필요하면 수동으로 옮길 것.

**[웹 Claude 지적 — v170, 규칙의 취약점] "홈으로 보내지 말 것"은
사람이 안 건드려야만 성립하는 규칙이다** — 비상정지 후 복구,
티치펜던트 수동조작, 노드 재시작 셋 다 로봇 자세를 바꾸므로
그때마다 같은 버그가 조용히 재발할 수 있다(로그는 "무게초과로
review_bin행"처럼 정상 처리된 것으로 보이지만 실제론 baseline이
틀린 것). 실측으로 10일차 아침 실제 재발함(관제PC 재부팅 후
로봇이 홈에 가 있었음, v168/v169 교신 참고). **대응: 시연
직전(또는 완주시험 첫 사이클로 겸용, 별도 시간 안 씀) 빈 캔
하나를 돌려본다** — `can_bin`으로 가면 정상, `review_bin`으로
가면 baseline이 틀린 것(원인 불문 결과로 잡힘). 30초 소요.
**근본수정(증상 대응이 아니라)은 시연 이후**: `post-pick`은
v117에서 고정 자세로 옮겼는데 `baseline`은 팔의 현재 자세에서
재는 구조가 그대로 남아 있다 — 이게 결함의 근원(중력보정 오차가
둘 다 같은 자세여야 상쇄됨). 다만 `baseline`은 예측 계산 이전
단계라 여기에 이동 동작을 넣으면 `extra_gap`이 늘어나는
부작용이 있다(v102에서 보류했던 것과 같은 구조) — 시연 이후
과제로 아래 "다음 순서"에 등재.

### [드디어 규명] 무게 드리프트 정체 — 로드셀 27초 주기 진동
벨트진동 아님(확인됨). 로드셀 자체가 주기 약 27초·진폭 약
±45g로 진동, 이 주기가 측정창보다 길어 평균/중앙값으로 원리적
제거 불가 — 이게 하루 종일 "원인불명 산포"의 정체였음.
`reset_workpiece_weight` 호출은 계속 금지(6일차 사고 전례,
시연 전날 리스크 과다) — 진동 근본조사는 시연 이후.

### [부분 완료] 도달한계 안전정지 — 최종 파라미터 (무정지는 미적용, 위 안전 절 참고)
```
정지마진        540mm (한계 590에서 50mm 앞)
워치독          120초 (전역, 정지사유 집합 tracking_node 내부)
칼만 vx 클램프   4.6~5.3mm/s (참값 4.98mm/s 기준, 7종 재검증 완료)
가로 경계 필터   vision.edge_margin_px=15px, 좌우 양쪽
```
검출 백로그 버그(`detection_min_gap_sec` 필터로 수정),
`hold`/`release` 명명, 타임아웃 트랙은 무시목록+파지후보 제외
전부 실물검증 완료. **다만 `conveyor.stop_during_pick`은 실제로
`true`로 남아있어(10일차 재확인, 위 안전 절 참고) 벨트는 여전히
파지 사이클마다 정지한다 — "무정지 전환 완료"라고 서술하지
말 것.** 도달한계 안전정지 기능 자체(위 파라미터)는 인터록과
별개로 구현·검증 완료 상태. **발표에서 "2.0cm/s"(벨트 문서값)
언급 금지** — 실측 벨트표면 약 5.0mm/s, 물체(비전) 약 4.98mm/s.

### [설계 확정, 미구현] 미지 클래스 3중 방어
0차 기동시 검증(model.names ↔ config 대조 — `gripper_profiles.
yaml`도 포함해야 함, plastic 프로파일 누락사고가 근거), 1차
트랙 미생성, 2차 라우팅조회 예외처리. 시연 후 구현.

### [완료 — 10일차] GPU 전환
**아침엔 드라이버가 "깨졌다"고 판단했으나 정정 — 실제 원인은
커널모듈 미적재(로딩 타이밍 문제)였다.** `.ko` 파일 자체는
`/lib/modules/7.0.0-31-generic/kernel/nvidia-595/`에 처음부터
있었음, 재빌드 불필요. 원래 GPU 컨테이너(`--gpus all`)가 아침에
기동 거부된 기전: prestart 훅(nvidia-container-runtime-hook)이
NVML로 GPU를 열거하려는데 nvidia 커널모듈이 미적재라 실패 →
컨테이너 프로세스 시작 전에 중단(GPU 요구는 생성 시점에 박혀
`docker start`로 못 바꿈, 그래서 아침엔 별도 CPU 컨테이너
`rocycle-yolo-cpu`로 우회했었음). 모듈이 `lsmod`에 정상 적재된
걸 확인한 뒤 원래 컨테이너로 전환 완료.

**실측 효과(10일차, 관제PC)**:
```
                CPU 모드   GPU 모드
전체 CPU(us)     45.6%      11.6%
검출 프로세스     698%       30%    (7코어 상당 -> 0.3코어)
GPU 사용률          0%        5%
VRAM                -    335MiB/8188MiB
검출 발행        7.42Hz     7.49Hz  (카메라 7.5Hz 상한에 묶여
                                    거의 안 변함 — 추론 자체는
                                    훨씬 빨라짐)
```
**CPU를 약 23배 덜 씀.** 발행주기가 카메라에 고정돼 있어
**우려했던 "GPU 전환 시 `min_consecutive_frames` 9→23 비례조정
필수" 문제 자체가 발생하지 않음** — 검출 발행 간격(따라서
9프레임이 뜻하는 실제 시간)이 GPU 전환 전후로 거의 그대로라
칼만 관측시간 문제가 생기지 않음(이전 우려는 "발행률이 오른다"
는 가정이었는데 실측상 카메라 상한 때문에 발행률 자체가 거의
안 오름). crop은 아직 미적용(속도이득 2.15배 유효, 시연 이후
재검증). **셸에서 파라미터 넘길 때 함정 있음 — 알려진 함정
절 참고**(`device:="0"`이 INTEGER로 해석돼 노드가 죽음).

**GPU 완주 1회 실물검증 완료(10일차, 사용자 지시로 여기서 마무리)**:
7종 전부 파지실패/도달한계/PLACECHK오탐 0. **주목할 결과 — 빈
캔 net -97.6g(오늘 관측된 최대 음수)도 can_bin으로 정상 처리**
— 9일차 밤에 넣은 `net_min_kg=-0.15kg` 하한이 없었으면 옛
-30g 규칙에 걸려 review_bin으로 잘못 갔을 값. 발표자료에 이
수정의 효과를 보여주는 사례로 쓸 만함. `[PLACECHK]` 5건 전부
정답(그중 -0.2915는 오전 옛 규칙이었으면 오탐이었을 값 —
10일차 재설계의 효과가 실사이클에서도 확인됨).

**오늘(10일차) 완주 3회 종합**: 1회차(CPU, 7종 전부, 페트병
핸드오버 성공) / 2회차(CPU, 7종 전부, 장갑 미검출 흘려보냄) /
3회차(GPU, 7종 전부, 빈캔 net -97.6g도 can_bin) — **3회 전부
파지실패 0·도달한계 0.** 빈 캔 net이 -97.6/-22.1/+44.3g로
142g 폭에서 흔들렸으나(로드셀 27초 주기 진동 범위) 전부
can_bin으로 일관되게 처리 — 임계 200g 대비 충분히 안전.

VRAM 8GB는 `nvidia-smi` 실측으로도 8188MiB 확인(앞서 PCI BAR
추정치와 일치).

### [팀결정, 최종] 시연 물체 구성 — 7개 물체(장갑 포함), paper 완전 제외
**[표기 정정]** 이전에 "6개"로 적었던 건 셈 실수 — 아래 표는
7행이고, 장갑(미학습)까지 포함해 7개가 맞음.
```
빈 콜라캔 355ml         → can_bin
샌드 캔 490ml(400g)      → 무게 차단 → review_bin
건전지 AA               → battery_bin
컵 뚜껑                 → plastic_bin
빈 라벨 페트병           → 사람에게 전달(human_handoff)
검은 비닐               → review_bin
장갑(미학습, 반드시 펴서) → 흘려보냄(일반쓰레기)
```
**paper 완전 제외 이유**: 초콜릿박스가 화면상 x위치(cx≈920
경계)에 따라 pet_labeled/paper 결정경계 위에 있어(양쪽 신뢰도
0.92~0.95로 확신) 조명·자세·리얼센스 무관하게 통제 불가 확인
— 4번의 재정정 끝에 확정(경위는 실측기록.md 9일차 밤 "종이
원인 규명 4번 뒤집힌 경위" 참고, 최종보고서용 사례). **장갑은
반드시 펴서 배치**(접으면 cx 200~399에서 `paper`로 파지트리거
됨 — paper_bin 미사용이라 엉뚱한 통행). **미학습물체 대본**:
"예상과 다르게 분류돼도 안전장치(무게게이트 또는 확인필요통
자동회수)가 최종적으로 처리한다"로 일반화 서술(정확한 시퀀스
재현은 단정 안 함). 찌그러진 캔 사용 금지(plastic 오검출 위험).
**[하지 말 것 — v168/v170 합의]** "혹시나" 하고 paper를 다시
테스트하지 말 것(4번의 재정정 끝에 확정된 결론, 재확인 불필요) —
그리퍼 힘(can 15N/plastic 15N 등)도 어젯밤 검증된 값 그대로
재조정하지 말 것.

### [진행 중] 신뢰도 분포 실측
**"신뢰도 0.6 미만은 검출로 인정하지 않는다"**가 정확한 표현.
**실측값 인용 시 "파지 트리거 구간에서"라는 조건을 반드시
명시할 것**(x위치 의존성 발견으로 인해 — 과대청구 금지).

### [종료 시점 최종상태] 리얼센스 4기능
```
= 회전각추정(고정캠 담당) / 파지자세검증 / depth높이측정 / 배치확인
(컨베이어 속도 동적조정은 4건에서 제외)
```
원래 10시간+ 다일간 설계(실측기록.md 8일차 스냅샷 참고).

**파지자세검증(손목캠) — [정정, 철회] 배경노출비율 지표는
틀렸음.** 실패표본이 실제로 나온 뒤 4건 중 1건만 맞음(원인:
파지 실패 시 물체가 그리퍼 바로 아래 벨트에 그대로 남아,
"물린 물체"와 "빈 그리퍼 아래 놓인 물체"가 손목캠엔 거의
같은 그림 — 처음 모은 빈손 표본이 벨트가 깨끗한 상태였어서
이 경우를 대표 못 했음, 표본설계 오류). 채도/밝기/표준편차/
엣지도 전부 겹침, depth도 안 됨(검은 비닐은 IR 흡수로 거리
안 잡힘). **코드에서 판정 문구 제거, 지표만 기록**(근거 없는
판정이 로그에 남으면 다음 사람이 믿게 됨 — 좋은 판단). 다음
설계 후보: 물체가 아래에 있을 수 없는 전용 검사자세로 옮겨
촬영(사이클시간 증가, 설계결정 필요, 미착수).

**배치확인(개방폭, PLACECHK) — [정정 — 10일차, 표본 4→11건]
"4/4 정답"은 표본이 좁았던 결과, 오탐 발생 후 판정방식 자체를
교체.** 설계문서의 `/onrobot/pose`는 못 씀(항상 고정값 반환,
실측으로 확인) — 대신 `/onrobot_joint_states`(개방폭) 사용.
10일차 완주시험 중 옛 설계("기준값 -0.4793 ± 허용폭 0.15")로
오탐 1건 발생, 11건 실측 재분석 결과:
```
배치 성공(열림)  -0.4793 ~ -0.2856 (폭 0.194, 기존 가정보다 넓음)
파지 실패(빈손)   +0.1622
```
두 군집 간격 0.448 — **"중심±허용폭" 대신 단일 경계값
`open_max_rad = -0.06`(중간값)으로 교체**, 과거 11건 재판정
11/11 정답, 이후 실사이클 오탐 0건. **발표에서 "4/4"는 쓰지
말 것 — 폐기된 값.** `plastic`의 `grasp_fail_joint_rad=null`
(찌그러진 뚜껑 관절각 0.78rad 편차) 문제와는 여전히 별개 —
이번 재설계로도 해소된 것은 아님, 그대로 유효.

**회전각 추정 — [중대] 전제 자체가 무너짐, 2단계 보류(사용자
결정).** 같은 490캔을 각도만 바꿔 측정: 벨트와 나란히 77/77
검출, 45도 비스듬히 0/77, 가로지름 1/77(같은 프레임의 뚜껑/
건전지는 세 번 다 77/77로 안정 — 카메라·조명 문제 아님).
**비스듬한 물체는 애초에 검출이 안 되므로 그리퍼를 돌릴 대상
자체가 없음** — 학습데이터가 캔을 벨트-나란히 자세로만 찍었을
가능성 큼(추정), 근본해결은 재학습(오늘 밤 범위 밖). **사용자
결정: 2단계(rz 적용)는 진행 안 함, 1단계(추정기 자체)만 과제
기록용으로 유지, 내일 시연에서 보여줄 필요 없음.** 추정기
자체 동작은 확인됨(3회 일관: 뚜껑 종장비1.08 원형자동배제,
캔2.79≈실측168/66=2.5, 건전지2.90, 각도-5.0~-5.7°).

**[촬영 체크리스트, 중요 추가] 캔은 반드시 벨트 방향과 나란히
놓을 것** — 비스듬하면 검출 자체가 안 돼 로봇이 아예 못 잡음
(회전각 추정과 무관하게 이번 시연에도 그대로 적용되는 제약).
장갑은 반드시 펴서(위 시연구성 절 참고). **[v167] 벨트에 테이프로
표시할 것 — 가로 위치뿐 아니라 정렬 방향까지 함께**: (1) 물체를
놓을 가로 위치, (2) 캔의 축이 벨트 진행방향과 나란한지(x위치
의존성 발견에 따른 조치, 초콜릿박스 사례와 같은 원리).

**[결정 — v167, 최종] PC 분리는 안 함, 발표는 정직한 서술로
대체.** 애초 "경합 해소" 명분은 오늘 밤 A/B 실측으로 이미
기각됨(리얼센스 가동이 C270 검출에 영향 없음 확정) — CLI가
반론, 웹 Claude가 수용. 사용자 확인 결과 분리를 원했던 실제
이유는 **"발표에서 2대 구성이라 말하려면 실제로 2대여야
한다"**(옵션 b) — 기술적 필요가 아니라 발표 정직성 문제였음.
**해결: 실제로 나누지 않고 정직한 서술로 발표**(사용자 확정):
"현재는 한 대에서 운영합니다. 초기 설계는 관제PC/로봇PC 2대
분산이었고, 나누면 장애가 격리되고 여러 라인으로 확장할 수
있습니다. 다만 저희 구성에서는 C270과 리얼센스의 USB 경합이
실측으로 확인되지 않아 분리의 실익이 없었습니다." — "안
했다"가 아니라 "재보고 안 해도 된다고 판단했다"로 서술.
depth 스트림을 실제로 켜서 쓸 일이 생기고 그때 C270 간섭이
실측으로 확인되면, 그 시점에 최소분리(리얼센스 노드만 별도
PC로)를 재검토(시연 이후).

**배치확인/depth 1단계는 미착수** — 이번 시나리오에서 안전·
판정에 안 걸려 후순위, 시간 종료로 못 함.

**미착수(세션 종료 시점)**: Final 폴더 UI 통합(pull은 완료),
파지검증 2단계, 저장소·실행사본 구조 정리.

### [재발견] 관절 특이점 36회
battery_bin 국한 아니고 여러 동작에서 고르게 발생, 컨트롤러
자동회피로 기능적 실패 없음(시연 화면엔 불안하게 보일 수 있음).
티치포인트 재설계는 시연 이후.

### 저장소 구조적 정리 필요 (시연 이후)
저장소 사본(`Rocycle/rocycle_robot/`)과 실행 사본(`ros2_ws/
src/`)이 다수 파일에서 갈라져 있음(`routing.py`/`tracking_
node.py`/`pickup_timing.py`/`command_mapping.py` 등, 한쪽에만
있는 파일도 있음) — 오늘 밤은 검증된 변경만 골라 커밋, 구조적
차이는 안 건드림(어느 쪽이 최신인지 확인 없이 맞추면 작업
유실 위험). Final/ 폴더는 DB/UI 담당자가 이미 사용 중(웹
API·DB브릿지·통계페이지 추가), 실제 운영 코드와는 독립.

**[완료 — 10일차] 무백업 위험 2건은 해소, 구조 정리는 여전히
남음.** v169 아키텍처 조사에서 `object_detection/
recycle_detection.py`(git에 전혀 없음)와 `conveyor_node.py`
(저장소본과 실행본이 270줄 차이)가 관제PC 디스크에만 있는
유일 사본이라는 게 드러나 긴급 백업함 — 기존 파일을 덮어쓰지
않고 `control_pc/` 하위 새 경로로 추가(커밋 `d1b7d60`). 같은
작업 중 그날 만든 미커밋 코드수정 124줄(`/ui/detections` 신설
+ `reach_limit_px` 추가, PLACECHK 판정방식 교체)도 함께 발견돼
사용자 승인 후 같이 올림(커밋 `82d0fae`). **다만 "어느 쪽
구현이 맞는지"(git 사본 vs control_pc/ 실행본) 판단과 병합은
여전히 안 함** — 시연 이후 구조 정리 때 함께 처리.

### 다음 순서 (시연 이후)
1. 저장소·실행사본 구조 정리(위 참고)
2. 벨트속도 4.98mm/s와 벨트-물체 관계, 로드셀 진동 근본원인
3. 그리퍼서비스 응답지연(770ms) 원인
4. 미지 클래스 3중 방어 구현
5. GPU 전환(`header.stamp` 선행) + crop 묶음 재검증
6. 관절특이점 원인조사·티치포인트 재설계
7. 리얼센스 4기능 나머지(파지자세검증 2단계, 배치확인, depth,
   회전각) — 실측기록.md 8일차 스냅샷 순서 참고
8. 신뢰도 분포 실측 마무리, 핸드오버 10회 카운트 잔여
9. eye-in-hand 캘리브레이션(과제 요건 확인되면)
10. **baseline 측정 자세 고정** — `post-pick`처럼 baseline도
    고정 자세에서 측정하도록 변경(현재는 팔의 현재 자세에서
    측정해 중력보정 오차가 상쇄 안 됨, v170 지적). `extra_gap`
    증가 부작용 처리 필요(v102와 같은 구조) — 원칙 참고.

상세 실측 경위·정정·철회 이력은 `실측기록.md` 참고(8·9일차분
전체 원본 스냅샷 3개 포함, 이 문서의 요약이 아니라 원문).
